# Adversarial review 2026-08-25: dispositions

The independent adversarial review (report:
`~/.claude/assets/reports/20260825-kanban-adversarial/independent.md`, 14
findings, 11 reproduced live) was worked the same day. Every finding is
dispositioned below. Verification for each fix was run in the live app on
:5106 unless marked otherwise, and the review's own measurement was repeated
where one existed.

| # | Finding | Disposition | Commit | Verified by |
|---|---------|-------------|--------|-------------|
| H1 | Column drags compose wrong from the 2nd drag; handlers double | **Fixed**: wireReorder guards per node, drag state on the container | 37d406e | 3 synthetic drags: commits 1/1/1 (was 1/2/4), every landing correct |
| H2 | Composer undo dead from the 2nd card | **Fixed**: attachBuffer re-keys its one binding; guard never cleared | 37d406e | 3 opens add 2 listeners total (was 2/open); undo→"first", redo→"second" |
| + | (found while fixing H2) attachStash stale closure wrote card B's text into card A's draft slot | **Fixed**: same re-key treatment | 37d406e | test-editor.sh 10/10 |
| M1 | C5/C6 charter checks cannot fail; C5 false in tree | **Fixed**: all write paths route through afterWrite, answers patch is a re-read; checks parse aliases, read afterWrite's body, pin the invalidation ledger, count live code only | 5e2ddf8 | all four review mutations now go red; tree restores green |
| M2 | Navbar permanently tight | **Fixed**: identity zone takes a modest basis and grows, capped at 44% | 6cff991 | 1920 un-tight with path and tab labels; tight fires only under pressure |
| M3 | Status band clipped 106px, no scroll, no sign | **Fixed**: band scrolls, wears the shared fade mask, keeps a chip floor | 6cff991 | 1440: fade shows, chips reachable |
| M4 | `.stat-chip span span` matches nothing | **Fixed**: chips carry .sn/.sl; shed rule targets .sl | 6cff991 | tight at 1440: numbers kept, words dropped, mute hidden |
| M5 | Sidebar toggle stale on 3 of 4 callers | **Fixed**: shared aside handle; toggleSide repaints | 3dac24e | the `\|` key and a direct call both repaint chevron, aria and tooltip |
| M6 | Hub leaks listeners plus 3 fetches per view switch | **Fixed**: navbar mount aborts the previous mount's wiring | aec979e | 1 live handler after 4 switches; 3 fetches per focus (was 15) |
| M7 | Ask deep links land on Boards | **Fixed**: one `?view=` parameter; hub reads the fragment, scrolls to and lights the row | aec979e | `/?view=asks#1pbj8e0o` lands on Your asks, row centered and lit |
| L1 | Pre-existing notes and tags flash as arrivals | **Fixed**: fxKeys namespaced per container AND card (three containers shared a bare card id and diffed against each other) | 5f5a5e9 | two 5-card walks: zero false arrivals (was 6) |
| L2 | "Could not load" rendered as "nothing there" | **Fixed**: search sentence names only kinds that answered; no-results names the unreached; switcher distinguishes unloadable from empty | 5f5a5e9 | code and helper logic; live simulation of a failed kind not run |
| L3 | Grip capture failure strands the drag | **Fixed**: listeners ride the window for the drag | 5f5a5e9 | window-only synthetic moves track; pointerup off the band ends cleanly |
| L4 | Sidebar missing the resize re-clamp | **Fixed**: mirrors the drawer's | 5f5a5e9 | setter clamps 2000→576 at 1440; resize re-clamps to 40% |
| L5 | Tag rename commits on click-away against a dead card, discards on Escape | **Fixed**: close paths run a cleanup; click-away flushes and saves (the field's own promise), Escape discards | 5f5a5e9 | stubbed tagOp: away = 1 commit "newname"; Escape = 0 commits |

Also fixed in the same pass, owner-reported rather than review-found: `.sband`
and `.sdelta` still wore the retired second-row styling inside the navbar
slot, so any board with news (apiservice) stacked the bar to 170px. One
sliding line now. The delta label words shed under tight, the board name keeps
an 8ch floor, and the All-boards prefix sheds before the name does. Swept at
900/1200/1440/1920 across two boards, the hub and drafts: one-line bar, no
page overflow (commit 5f5a5e9).

Out-of-scope observations from the review, acknowledged and left standing:
the TDZ category found nothing (with the reviewer's stated caveat), and the
wireReorder axis note (flexDirection computes "row" for non-flex elements)
stays a latent trap for a hypothetical fourth caller.
