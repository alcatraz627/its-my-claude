# The showcase board

Every card here exists to demonstrate one feature of the board. Each carries the
`showcase` tag plus a slug naming what it shows, so a UI review can find the
state it needs instead of hunting for a board that happens to have one.

## Backlog

- [ ] Tag kinds: a card wearing one of every tag hue at once
- [ ] Title brief: a deliberately long title that has to be cut down to a scannable name the eye can recognise without reading the whole sentence
- [ ] Execution order: this one comes after another card
- [ ] Verify executed: a claim backed by having run it
- [ ] Verify cited: a claim backed by a file and a line
- [ ] Verify reasoned: a claim backed by argument alone
- [ ] Needs human: a card the agent cannot close by itself
- [ ] Sub-items: a card with a checklist inside it
  - [x] a sub-item that is done
  - [ ] a sub-item that is not
  - [ ] a third, to show the count
- [ ] Linked doc: a card that opens a document
- [ ] Long body: a card whose claim runs past one line, so the face has to decide what to show and the drawer has to hold the rest of it comfortably

## Active

- [ ] Goal: a card with a one-line reason for existing
- [ ] Notes: a card carrying owner notes

## Blocked

- [ ] Blocked: a card waiting on something outside the board

## Done

- [x] Done: a card that has been closed
