# 2026-06-28 — L3 weekly audit

Surfaced: 25 · Approved: 0 · Applied: 0

## Proposal 1/25 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/opt-in-polarity.md`
- Intent:     Create new rule file for inverted-user-defined-opt-in-polarity
- Confidence: 0.75
- Rationale:  [S3, 2x] Pattern: agent acknowledges opt-in language in the request but codes the inverse default branch (feature activates by default instead of being off-unless-enabled). Has no dedicated rule file. Precheck is unambiguous: 'If user said opt-in to X, does the default branch SKIP X — not activate it?'
- Draft:
```
New file rules/opt-in-polarity.md: precheck gate, canonical before/after example (LOG_STRUCTURED=true means log only when set, not log unless unset), atone slug reference, link to rules/communication.md § escape-hatch. Frontmatter triggers: phrase:"opt-in", phrase:"only when enabled", phrase:"feature flag".
```
- Challenger: Two S3 events is the bare graduation threshold; may be project-specific and not worth a global rule file until a third occurrence confirms it as a repeating class.

## Proposal 2/25 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/communication.md`
- Intent:     Add named bullet for unprompted-infra-scope-creep under scope-control section
- Confidence: 0.72
- Rationale:  [S3, 2x] Adding CI/hooks/automation unprompted is a specific sub-case of scope-ceiling the general rule hasn't reliably prevented across two occurrences. Needs a named callout with concrete infra types to make the trigger recognizable before authorship.
- Draft:
```
+ **Infra scope creep:** Never add CI pipelines, git hooks, launchd plists, or automation scaffolding unless the user explicitly asked for it. 'No CI exists yet' is a scope constraint, not an invitation.
```
- Challenger: Already implied by scope-as-ceiling; enumerating specific infra types may miss future forms and risks making the rule feel like a whack-a-mole list.

## Proposal 3/25 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/scripts/hooks/speculative-export-check.sh`
- Intent:     Propose PreToolUse hook to catch speculative-abstractions at authorship time
- Confidence: 0.62
- Rationale:  [S3, 4x, 3rd recurrence despite live rule+atone+hints] The rule isn't preventing recurrence. The weak link: agent exports a function before running a caller grep. A hook on Write/Edit that detects a new export symbol in the diff and fires 'Did you grep for a caller first?' would catch it at the moment of authorship rather than post-correction.
- Draft:
```
New hook: speculative-export-check.sh — PreToolUse on Write/Edit, detects 'export (function|const|type) <NewSymbol>' where NewSymbol does not exist in prior file content, injects nudge: 'New export detected — have you grepped for a caller? Run: rg -n "NewSymbol" <project_root>/'
```
- Challenger: Write/Edit hooks scanning for new exports would fire constantly during legitimate refactors; false-positive rate is likely prohibitive without a very precise diff-scope filter.

## Proposal 4/25 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/scripts/hinters/`
- Intent:     Add Stop-hook hinter scanning for uncited authority-claim phrases
- Confidence: 0.60
- Rationale:  [S3, 11x] Highest-frequency recurring failure — structural-claim-without-reading-code. A Stop hook scanning the completed-turn assistant text for authority-claim phrases ('is the authority', 'is the source of truth', 'is the only writer', 'is the final check') and verifying a file:line citation pattern appears nearby would flag uncited claims before the user reads them.
- Draft:
```
New hinter: authority-claim-nudge.sh — Stop hook, scans assistant text for PHRASE_LIST, for each match checks whether a file:line pattern (\S+\.\w+:\d+) appears within 200 chars; logs warning for any uncited authority claim.
```
- Challenger: Stop hooks run after the turn completes; a post-fact nudge doesn't prevent the uncited claim from reaching the user — it only seeds the next turn with a correction prompt, which is weaker than a pre-send gate.

## Proposal 5/25 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/scripts/hooks/cli-gating.sh`
- Intent:     Fix cli-gating hook exclusion patterns to stop blocking read-only gh CLI calls and HEREDOC writes
- Confidence: 0.85
- Rationale:  6 false-positive misfires per digest across 7 consecutive days. The hook is blocking read-only gh commands (gh api GET, gh pr list, gh issue view, gh release list) and HEREDOC file writes where no gated CLI is present. These are the two documented misfire categories — add explicit exclusion patterns for both.
- Draft:
```
Add to cli-gating.sh: (1) skip block when command matches /^gh (api|pr list|pr view|pr status|issue list|issue view|release list|repo view|auth status)/ with no mutation flags (--create, --delete, --merge, --close, --push); (2) skip when the entire command is a HEREDOC write (<<'?EOF) with no gh/npm/pip invocation present.
```
- Challenger: The hook's intended full coverage scope isn't documented in the digests; widening exclusions without an audit of what it was originally meant to catch may silently permit legitimate violations.

## Proposal 6/25 — affirm-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/exercise-based-verification.md`
- Intent:     Add dependency-landing trigger as a named subsection
- Confidence: 0.78
- Rationale:  test-feature-when-dependency-lands (cluster I, affirmed, confirmed by ARG_MAX bug-catch pin) documents a specific failure window the general 'Run it' directive doesn't name: the gap between a blocking dependency landing and the first real run against it. The ARG_MAX class of bug is uniquely catchable in this window and nowhere else.
- Draft:
```
+ ## The dependency-landing trigger\n+ When a blocking dependency (model download, package install, API endpoint becoming available) lands, treat its arrival as an immediate run trigger — not as 'setup complete, continue.' Run the dependent feature against real data before moving to the next step. The ARG_MAX bug class (malformed invocation only surfaced against a real model) is cheapest to catch here.
```
- Challenger: The existing 'Run it' section already implies this broadly; adding a named subsection may add length without changing behavior for agents who read the rule carefully.

## Proposal 7/25 — affirm-analyst _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     Promote audit-file-character-before-applying-global-rule from Tier-2 to Tier-1 with a one-line brief
- Confidence: 0.65
- Rationale:  Cluster F (Audit-before-action) is the strongest affirm theme this week with 3 events across 14 days — higher repeat signal than most Tier-1 entries. Currently only in the Tier-2 pointer table. A one-line brief in Tier-1 ('Before moving code into an existing file or invoking a global rule on an unusual case, read the file's top ~20 lines to confirm the character fits') would increase per-session visibility for a well-working pattern.
- Draft:
```
Add to CLAUDE.md Tier-1 table row: audit-file-character-before-applying-global-rule | 'Before consolidating into an existing file or invoking a global rule on an unusual case, read the file top and name any tension — don't silently apply or silently skip' | → rules/audit-file-character-before-applying-global-rule.md
```
- Challenger: The Tier-0 and Tier-1 sections are already long; promoting another entry dilutes the signal. The rule is clearly working at Tier-2 — affirmed 3× means it doesn't need higher visibility to fire.

## Proposal 8/25 — affirm-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/communication.md`
- Intent:     Add scope-convention-to-one-consumer as positive example in scope-control section
- Confidence: 0.60
- Rationale:  scope-convention-change-to-one-consumer (cluster G affirm) gives a concrete positive principle: when changing a path/naming convention, scope the change to the consumer (matcher), not the shared primitive (helper). This is an important nuance of scope-as-ceiling that the abstract rule doesn't convey — it specifies WHERE the change lives, not just HOW MUCH changes.
- Draft:
```
+ **Convention scope example:** When changing a path convention, put the change in the consumer that interprets it (e.g. the S3 key matcher), not the shared primitive that generates it (e.g. org_s3_key). The primitive stays stable; only the consumer that triggered the need adapts.
```
- Challenger: Named examples in general rules can anchor agents to the specific surface (S3 paths) rather than the principle; the example may be too narrow for a global rule.

## Proposal 9/25 — affirm-analyst _→ skipped or rejected_

- Target:     `~/.claude/scripts/hinters/05-atone-tldr.sh`
- Intent:     Include top 2 current affirm events in session-start hinter output
- Confidence: 0.58
- Rationale:  The atone hinter injects only negative patterns at session start. Including 1-2 current affirm events ('keep doing: X, Y') alongside the corrections provides a calibration anchor — 'here is what is working' reduces over-correction into safe-but-stale avoidance patterns. The affirm TLDR already has the data; it just isn't surfaced.
- Draft:
```
After top-mistake-patterns block in 05-atone-tldr.sh, append: 'Affirmed-good behaviors to repeat: ✓ [1x] test-feature-when-dependency-lands — Run feature the instant blocking dep lands … ✓ [3x] audit-file-character — Read destination file top before consolidating.' Pull from affirm TLDR dynamically via atone.sh.
```
- Challenger: The hinter is already at attention-budget limits for most sessions; adding affirm signals may push critical atone patterns below the fold and reduce their corrective visibility.

## Proposal 10/25 — dreams-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/assume-before-verify.md`
- Intent:     Create meta-rule unifying the assume-before-verify cluster under a single precheck gate
- Confidence: 0.68
- Rationale:  Cross-domain association surfaced Day 5 and Day 7: assume-before-verify-cluster (atone) ↔ proactive-risk-surface (memory). structural-claim-without-reading-code, helper-return-type-assumption, and grep-scope-before-claiming-absence share one root: asserting external state without verifying it first. A single precheck ('Can I name the file:line / grep result proving this right now? If no — verify first.') with backreferences to the three files would reduce the cognitive surface agents need to internalize.
- Draft:
```
New file: rules/assume-before-verify.md — unified framing, single precheck gate, Related: structural-claim-without-reading-code, helper-return-type-assumption, grep-scope-before-claiming-absence, inverted-user-defined-opt-in-polarity. Tier-2 pointer in CLAUDE.md. Triggers: phrase:'is the authority', phrase:'source of truth', phrase:'there is no existing'.
```
- Challenger: A meta-rule pointing to four others may be harder to internalize than the individual rules; abstraction at the wrong layer can diffuse focus rather than consolidate it.

## Proposal 11/25 — dreams-analyst _→ skipped or rejected_

- Target:     `~/.claude/scripts/hinters/burst-session-nudge.sh`
- Intent:     Add hinter: offer /core-dump mini after 3rd session on same CWD in one day
- Confidence: 0.55
- Rationale:  Sessions data (every digest this week): 5+ sessions within 2 hours on spike days. After the 3rd session on the same CWD in one calendar day, proactively surface '/core-dump mini' to preserve state and reduce redundant context-reconstruction overhead on iterative-spike days.
- Draft:
```
New hinter: burst-session-nudge.sh — UserPromptSubmit hook, counts session_start WAL events for current CWD with today's date, fires once per day when count ≥ 3: 'You have opened N sessions on this project today — want a /core-dump mini to preserve state before context diverges?'
```
- Challenger: Session burst-clustering may be intentional iterative-spike work; an auto-nudge every 3rd session risks being intrusive on exactly the days the user is in a productive flow.

## Proposal 12/25 — dreams-analyst _→ skipped or rejected_

- Target:     `~/.claude/scripts/doctor.sh`
- Intent:     Add hook false-positive rate check to doctor.sh quarterly hygiene scan
- Confidence: 0.60
- Rationale:  cli-gating hook produced false positives every day for 7 days with no action triggered. Beyond the immediate fix, doctor.sh should detect hooks with anomalously high event rates (claude-audit events.jsonl count per slug over 7 days > N) and surface them as drift requiring human review — before they accumulate to 42-per-week without notice.
- Draft:
```
Add to doctor.sh: read ~/.claude/claude-audit/events.jsonl (or equivalent), count events per hook-slug in last 7 days, WARN if any slug exceeds 5 events: 'Hook <slug> fired <N> times in 7 days — likely false-positive cluster; review exclusion patterns.'
```
- Challenger: claude-audit events.jsonl and doctor.sh may not share a data path today; integration requires plumbing work that could introduce new failure modes.

## Proposal 13/25 — dreams-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/speculative-abstractions-without-a-load-bearing-caller.md`
- Intent:     Add co-occurrence escalation note: if speculative-abstractions AND inverted-opt-in fire same session, treat as method failure
- Confidence: 0.52
- Rationale:  2026-06-25 pin documented 3rd S3 recurrence of speculative-abstractions co-occurring with inverted-opt-in in the same session — a meta-failure signal: agent isn't reading scope intent at all. Adding a co-occurrence note makes the escalation path explicit: both patterns sharing a session means re-read the full original request before continuing.
- Draft:
```
+ ## Co-occurrence escalation\n+ If both this pattern and inverted-user-defined-opt-in-polarity fire in the same session, treat it as a method failure — not two independent bugs. Stop, re-read the user's full original request, and confirm scope intent before continuing any implementation.
```
- Challenger: Encoding co-occurrence rules from a single session's incident risks over-fitting to one data point; two co-occurrences would be a stronger signal.

## Proposal 14/25 — gcc-fitness-scorer _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     Move git, comments, and test-every-change Tier-0 paragraphs to Tier-1 brief+pointer
- Confidence: 0.55
- Rationale:  CLAUDE.md Tier-0 now spans 12+ dense paragraphs. git, comments, and test-every-change all have full rule files and are not 'silent failure / hard-to-discover' risks — they were originally inline for urgency, but the rule files are now mature. Moving them to Tier-1 with a one-line brief each would reduce session-start cognitive load while keeping hard-consequence rules (shell safety, file-path-period, state-verification, atone) inline.
- Draft:
```
Remove git/comments/test-every-change paragraph bodies from Tier-0. Add to Tier-1 table: git (commit cadence, no main push, public default → rules/git.md), comments (human-first, no plan refs → rules/comments.md), test-every-change (scale-to-task, clean-slate → rules/testing.md).
```
- Challenger: These were promoted to Tier-0 precisely because they were missed at Tier-1; demoting them may restore the same miss rate that motivated their original promotion.

## Proposal 15/25 — gcc-fitness-scorer _→ skipped or rejected_

- Target:     `~/.claude/rules/speculative-abstractions-without-a-load-bearing-caller.md`
- Intent:     Add reciprocal cross-link to generalize-before-enumerate.md in Related section
- Confidence: 0.70
- Rationale:  generalize-before-enumerate.md has a 'Boundary with the sibling rule' section describing the split well, but speculative-abstractions lacks a reciprocal pointer. An agent loading only the speculative-abstractions rule won't discover the sibling. Adding a Related entry makes both rules bidirectionally discoverable when only one is in context.
- Draft:
```
+ - `rules/generalize-before-enumerate.md` — fires when you have one real callsite but invent breadth it doesn't need; speculative-abstractions fires when you have zero callsites.
```
- Challenger: The files already have Related sections and the generalize-before-enumerate file explicitly names the boundary; adding one more cross-link is low-impact and unlikely to meaningfully change agent behavior.

## Proposal 16/25 — gcc-fitness-scorer _→ skipped or rejected_

- Target:     `~/.claude/scripts/validate-triggers.sh`
- Intent:     Extend validate-triggers.sh to detect orphaned rules/*.md files not referenced in CLAUDE.md or LOOKUP.md
- Confidence: 0.70
- Rationale:  Several rules (js-escape-sequences, cache-externally-mutated-state, never-modify-anthropic-credentials, etc.) are not in the CLAUDE.md Tier-2 table. An agent loading only CLAUDE.md won't discover them via any pointer. Extending the weekly validator to flag orphaned rule files prevents silent coverage gaps as new rules are added.
- Draft:
```
Add to validate-triggers.sh: list all rules/*.md, check each basename appears in CLAUDE.md Tier-2 table or LOOKUP.md, print ORPHANED: <filename> for any that don't. Run as part of validate-all.
```
- Challenger: Not all rules need Tier-2 entry — narrow-scope rules are better discovered via atone TLDR at session start; an orphan check may generate false-positive noise for intentionally low-visibility rules.

## Proposal 17/25 — gcc-fitness-scorer _→ skipped or rejected_

- Target:     `~/.claude/features/proposals.md`
- Intent:     Add challenger_note as a documented optional field in the proposals schema
- Confidence: 0.65
- Rationale:  i-dream proposals (proposals.jsonl) have no structured counter-argument field. Adding challenger_note as a documented optional field in the propose.sh template would make weekly reviews faster (user can filter challenged vs unchallenged proposals) and reduce under-examined proposals becoming standing rules.
- Draft:
```
Add to propose.sh: --challenger-note <text> optional flag. Add to proposals.jsonl schema: challenger_note string|null. Update features/proposals.md § Schema to document it. Challenger note appears in `proposals review` output.
```
- Challenger: Meta-proposal about proposal format is better addressed in the i-dream weekly audit prompt itself rather than permanently encoding it in the proposals schema.

## Proposal 18/25 — graduation-curator _→ skipped or rejected_

- Target:     `~/.claude/rules/opt-in-polarity.md`
- Intent:     Graduate inverted-user-defined-opt-in-polarity [S3, 2x] into new rule file with precheck gate
- Confidence: 0.75
- Rationale:  Pattern is distinct enough to warrant a dedicated file: agent reads 'opt-in to X' but implements 'X is on unless disabled' (the inverse default). Two S3 occurrences with atone slug is the minimum graduation bar. Precheck: 'If user said opt-in to X, does the default branch skip X?' Canonical example: LOG_STRUCTURED=true means activate when true, not deactivate when absent.
- Draft:
```
New file rules/opt-in-polarity.md: frontmatter triggers (phrase:opt-in, phrase:only when enabled, phrase:feature flag, phrase:behind a flag), precheck gate with yes/no outcome, before/after code example, Related: rules/communication.md, atone slug inverted-user-defined-opt-in-polarity.
```
- Challenger: Two S3 events at the graduation threshold; a third recurrence would provide stronger evidence this is a repeating class before committing to a dedicated file.

## Proposal 19/25 — graduation-curator _→ skipped or rejected_

- Target:     `~/.claude/rules/communication.md`
- Intent:     Graduate unprompted-infra-scope-creep [S3, 2x] to named bullet with concrete infra callout
- Confidence: 0.72
- Rationale:  The general scope-ceiling rule hasn't prevented this sub-case across two S3 occurrences. The infra pattern (CI, hooks, automation) needs a named trigger with concrete surface types so the agent can recognize it before authoring the scaffolding — not after being corrected.
- Draft:
```
+ **Infra scope creep:** Never add CI pipelines, git hooks, launchd plists, pre-commit configs, or automation scaffolding unless explicitly asked. 'This codebase has no CI' is a scope constraint to respect, not an opening to fill. Graduating from atone slug unprompted-infra-scope-creep [S3, 2x].
```
- Challenger: Explicit enumeration of infra surface types may miss future forms (e.g. Terraform, GitHub Actions vs Jenkins); the abstract scope-ceiling rule is more durable than a list.

## Proposal 20/25 — graduation-curator _→ skipped or rejected_

- Target:     `~/.claude/rules/exercise-based-verification.md`
- Intent:     Graduate test-feature-when-dependency-lands [affirm, cluster I] to named section in exercise-based-verification
- Confidence: 0.74
- Rationale:  Affirm cluster confirmed by concrete pin with a real bug (ARG_MAX on new model invocation). The dependency-landing window is a distinct trigger not covered by the general 'Run it' directive and the bug class is uniquely catchable there. Encoding it with a named trigger makes the rule actionable at the moment a dependency lands rather than only at final integration.
- Draft:
```
+ ## Dependency-landing trigger\n+ When a blocking dependency (model download, package install, API becoming reachable) lands — treat its arrival as a run trigger, not a setup step. Exercise the dependent feature against real data immediately. Bugs in this window are cheapest to catch here and invisible to later integration tests (the ARG_MAX class: bad invocation format only surfaces against a real artifact).
```
- Challenger: The general 'Run it' section already implies immediate execution; a named subsection may add cognitive load for a narrow trigger without changing behavior for careful readers.

## Proposal 21/25 — graduation-curator _→ skipped or rejected_

- Target:     `~/.claude/rules/communication.md`
- Intent:     Graduate scope-convention-change-to-one-consumer [affirm, cluster G] as positive example in scope-control
- Confidence: 0.60
- Rationale:  The affirm event encodes a reusable principle: when a convention changes, scope the change to the consumer (the matcher that interprets it), not the shared primitive (the helper that generates it). The abstract scope-ceiling rule doesn't distinguish WHERE within the codebase the change should land — this example fills that gap.
- Draft:
```
+ **Convention scope (positive example):** When changing a path or naming convention, put the change in the matcher/consumer, not the shared primitive that defines the convention. The primitive stays stable; the consumer adapts. (Affirm: scope-convention-change-to-one-consumer, cluster G.)
```
- Challenger: Named examples in general rules anchor agents to the specific surface (S3 path matchers) and may not generalize; the abstract rule is more durable without the example.

## Proposal 22/25 — abandoned-threads _→ skipped or rejected_

- Target:     `~/.claude/scripts/hooks/cli-gating.sh`
- Intent:     Escalate cli-gating hook false positives to immediate action — 7 days without fix
- Confidence: 0.88
- Rationale:  6 false-positive misfires per digest × 7 consecutive days = 42+ obstructive misfires this week with no proposal, no fix, no close. The hook is blocking read-only gh calls and HEREDOC writes — both documented and uncontroversial false positives. This is the definition of an abandoned thread. Escalate: run the exclusion-pattern fix (proposal #5) or temporarily disable pending a rewrite.
- Draft:
```
Immediate action: `grep -n 'BLOCK\|DENY' ~/.claude/scripts/hooks/cli-gating.sh` to audit current match logic, then add exclusion patterns per proposal #5. If fix is not feasible within this session, disable hook via touch ~/.claude/.no-cli-gating and log the disable in WAL.
```
- Challenger: The digests may be replaying the same stale batch of past events rather than new daily occurrences; verify via claude-audit whether fresh false positives are happening in the last 48 hours before treating as ongoing.

## Proposal 23/25 — abandoned-threads _→ skipped or rejected_

- Target:     `~/.claude/subconscious/dreams/open-threads.jsonl`
- Intent:     Close or action burst-clustering session pattern thread — appears every digest, no action taken
- Confidence: 0.65
- Rationale:  Burst-clustering signal (5 sessions in 2 hours) has appeared in every daily digest this week with no proposal actioned and no explicit close. An open thread with no action is noise in the digest — it crowds out higher-signal items. Either action proposal #11 (burst-session hinter) or explicitly close with rationale: 'expected iterative behavior, no rule needed.'
- Draft:
```
Close thread with: 'Acknowledged as expected iterative spike behavior. No rule needed. Revisit if user explicitly flags context-loss as a problem on burst days.' OR merge into proposal #11 and mark proposal as the action.
```
- Challenger: Closing a thread without action may be the correct resolution; the signal is observational, not necessarily a problem requiring intervention.

## Proposal 24/25 — abandoned-threads _→ skipped or rejected_

- Target:     `~/.claude/rules/speculative-abstractions-without-a-load-bearing-caller.md`
- Intent:     Flag speculative-abstractions mechanical enforcement as stale — rule exists, hook pending, 3rd recurrence happened
- Confidence: 0.80
- Rationale:  Rule exists since at least 2026-05-16. Hook has been 'proposed but not shipped' for at least 2 weeks (pin-20260624080929-6d documents 3rd recurrence despite rule+hints). The gap between 'rule exists' and 'hook deployed' is now a known-open item. Either ship proposal #3 or document in the rule file why the hook is impractical, closing the pending-action state explicitly.
- Draft:
```
Action A: ship speculative-export-check.sh with tuned false-positive suppression (only fire on new symbols in new files, not on refactored exports). Action B: add to rule file under ## Enforcement: 'No mechanical hook — false-positive rate on export-pattern matching is prohibitive. Rely on precheck + atone escalation path.' and close pending-action state.
```
- Challenger: If the false-positive rate genuinely makes a hook impractical, documenting that assessment is more honest than leaving it as perpetually-pending; Action B may be the right close.

## Proposal 25/25 — gcc-fitness-scorer _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     Add auto-memory project path pointer to Tier-1 Memory tiers section
- Confidence: 0.55
- Rationale:  The auto-memory system (projects/<cwd>/memory/MEMORY.md) is loaded via system-reminder for interactive sessions but not referenced anywhere in CLAUDE.md. Sub-agents and background agents dispatched without the full system-reminder context won't know to check it. A one-line pointer in the Tier-1 Memory tiers brief would make project memory access reliable across agent types.
- Draft:
```
In Tier-1 Memory tiers brief, add: 'Project auto-memory: ~/.claude/projects/<cwd-hash>/memory/MEMORY.md (loaded via system-reminder for interactive sessions; sub-agents should read it explicitly when dispatched on this project). Global: ~/.claude/memory/global/.'
```
- Challenger: Memory is described in Tier-1 already and system-reminder handles injection for interactive sessions; adding a path pointer may confuse sub-agents about which memory system to consult when both exist.

