# 2026-07-26 — L3 weekly audit

Surfaced: 12 · Approved: 7 · Applied: 7

## Proposal 1/12 — graduation-curator _→ rejected 2026-07-28_

- Target:     `~/.claude/rules/parity-before-purge.md`
- Intent:     Graduate rebuild-replaced-accumulated-ux-without-parity-audit (3x S3) to a standalone rule requiring a parity ledger before any bulk replacement or purge of user-facing UI components
- Confidence: 0.72
- Rationale:  3x S3 events meet the graduation threshold. The pattern is currently embedded only as a provenance example in invariant-graduation.md, not as a standalone actionable rule with its own precheck and diagnostic signal. The doc-22 incident demonstrates the mechanism: plan language asserts threading, no parity ledger is created, successor session rebuilds instead of threads, feedback-hardened UX is lost. A dedicated rule file with an explicit precheck gates the failure mode independently of the broader invariant-graduation trigger.
- Draft:
```
NEW FILE ~/.claude/rules/parity-before-purge.md: Before purging or replacing an existing user-facing component, assemble a parity ledger listing each behavior the old component carried and the artifact proving the replacement covers it. Diagnostic signal: you are about to delete or rebuild a component that has accumulated multiple review cycles of user feedback — the parity ledger is the proof of continuity.
```
- Challenger: invariant-graduation.md already covers the mechanism with Standing-constraints entry + Task-tool task; a second rule file for the same failure mode splits the reader across two files and risks the rules drifting on re-edits.

## Proposal 2/12 — affirm-analyst _→ applied 2026-07-28_

- Target:     `~/.claude/rules/communication.md`
- Intent:     Add arm-the-receiver-before-ending-the-turn as a named clause in the Escape hatch section
- Confidence: 0.75
- Rationale:  3 cluster-I affirm events form a coherent pattern: when ending a turn that set up an async wake, the receiver must be armed before the turn closes or the wake signal fires to nothing. User explicitly praised the outcome. The 43h idle-to-wake success is the concrete evidence. No existing rule covers this cross-domain pattern (IPC, sub-agent completion, scheduled wakeup). Communication.md's escape hatch section already lists pause conditions — adding this is proportionate without a new file.
- Draft:
```
+ Arm the receiver before ending the turn. When a turn sets up an async handoff (IPC listener, Monitor, scheduled wakeup, sub-agent completion notice), confirm the receiver is armed before stopping. A wake signal that fires to no listener forces a re-run and may require a full context restore.
```
- Challenger: This pattern arises in a small fraction of sessions (IPC/watcher handoffs); embedding it in the general communication escape hatch may add scope-confusion for sessions where it never applies.

## Proposal 3/12 — atone-analyst _→ applied 2026-07-28_

- Target:     `~/.claude/rules/testing.md`
- Intent:     Add [design-mocks] as a named precheck tag in the UI/frontend verification section
- Confidence: 0.63
- Rationale:  2x S3 with SessionStart flagging it as 'still recurring despite this reminder — this is a blind spot, slow down.' While below the 3x standalone-rule graduation threshold, adding a targeted [design-mocks] tag to testing.md's UI section anchors the check to the rule surface agents read before UI work, complementing the per-session hook. The 'blind spot' annotation suggests the hook alone is insufficient.
- Draft:
```
+ [design-mocks] Before implementing any user-facing UI feature (labels, creation flows, module structure, form layouts), grep for design mocks or Figma specs for the surface. If they exist, consult them before writing a line of UI code. Shipped UI that ignores existing mocks is a known S3 recurrence.
```
- Challenger: Only 2x, below graduation threshold; the SessionStart hook already surfaces this per-session, so adding to testing.md creates two sources of the same instruction that can drift on re-edits.

## Proposal 4/12 — atone-analyst _→ rejected 2026-07-28_

- Target:     `~/.claude/rules/contain-subagent-token-sprawl.md`
- Intent:     Add a doc-identity verification precheck before dispatching any expensive (opus/fable) agent against a named document
- Confidence: 0.65
- Rationale:  The wrong-authority-doc incident (mist-20260723-100023-c1): session-accumulated shorthand caused an expensive fable sub-agent to audit concepts.md (Claude-authored, downstream of the app) instead of the canonical contract at versable-builder/user_docs/speedway-cprd.md. The audit was circular. Expensive agent dispatches are the highest-cost surface for this mistake; adding a precheck here gates the failure at the highest-leverage point.
- Draft:
```
+ Verify doc identity before dispatch. Before sending an expensive (opus or fable) agent to audit or review a named document, resolve the path with fd/find and confirm it is (a) the canonical human-authored source and (b) not a Claude-generated downstream artifact. Session shorthand accumulated over multi-day work is a known drift vector.
```
- Challenger: This mixes doc-verification logic into a token-cost rule, coupling two distinct concerns; a better home is structural-claim-without-reading-code.md (the process-completion-claims clause), which already addresses verify-identity-before-asserting-authority.

## Proposal 5/12 — dreams-analyst _→ applied 2026-07-28_

- Target:     `~/.claude/rules/structural-claim-without-reading-code.md`
- Intent:     Add a derived-vs-canonical document provenance check to the trigger phrase list — when asserting a doc is 'the spec' or 'the authoritative source,' also verify it is human-authored and not downstream of the application
- Confidence: 0.60
- Rationale:  The wrong-authority-doc incident is a sub-class of structural claim where the doc's provenance is wrong, not just its path. The existing rule covers 'name the file:line that proves the claim' but not 'verify the claimed source is not itself Claude-authored.' An auditor that reviews a Claude-generated doc as the spec produces a circular review — one the structural-claim precheck would not catch.
- Draft:
```
+ When asserting a document is 'the spec', 'the contract', or 'the source of truth': also check its provenance — is it human-authored or Claude-generated documentation downstream of the application? A Claude-authored doc treated as a spec produces a circular review.
```
- Challenger: The structural-claim rule is already at S3 12x and has a complex precheck; the 'expand trigger phrase list' proposal was rejected 2026-07-10 — this derived-vs-canonical addition is semantically adjacent to that rejection and may be caught by the same filter.

## Proposal 6/12 — gcc-fitness-scorer _→ rejected 2026-07-28_

- Target:     `~/.claude/scripts/hinters/persona-suggest.sh`
- Intent:     Diagnose and redesign or remove the persona-suggest hook — 111 fires with 0 tracked heedances in W30 makes it the highest-volume zero-compliance hook in the batch
- Confidence: 0.67
- Rationale:  W30 telemetry: persona-suggest fired 111 times, 0/0 tracked heedances. At zero heedance across 111 invocations the hook is either not actionable in its current form, has broken telemetry, or is consistently ignored. Non-actionable hooks at this volume erode agent attention to hooks that do matter. A spot-check on 5 recent fire events will identify the failure mode: wrong trigger (tighten regex), untracked (fix telemetry write), or genuinely ignored (redesign instruction or de-register).
- Draft:
```
Sample 5 recent persona-suggest fire events from the W30 log, classify each as correct-fire-heeded, correct-fire-ignored, or false-positive. If over 60% are FP or ignored: either tighten the trigger or de-register from settings.json.
```
- Challenger: Zero tracked heedances does not equal zero actual heedances — if the heedance-write in the hook script has a bug, all real heedances are silently lost; run the telemetry audit before concluding the hook is non-actionable.

## Proposal 7/12 — gcc-fitness-scorer _→ applied 2026-07-28_

- Target:     `~/.claude/logs/`
- Intent:     Audit the hook heedance tracking pipeline — prefer-ripgrep (87 fires, 0 tracked), safe-delete (fires, 0 tracked), and persona-suggest (111 fires, 0 tracked) all show the same zero-heedance pattern simultaneously in W30
- Confidence: 0.68
- Rationale:  Three hooks across distinct categories (search, delete, persona) all report zero tracked heedances in the same W30 window. Simultaneous zero compliance across all three is far less probable than a broken heedance-write pipeline. If tracking is broken, every weekly audit that uses heedance data as a proxy for hook effectiveness is flying blind on which hooks are working.
- Draft:
```
Check: does ~/.claude/logs/hook-telemetry.jsonl (or equivalent) contain any heedance:true entries in the W30 window? If not, identify the write path in each affected hook script and confirm it executes correctly. Cross-reference one known-compliant hook as a control.
```
- Challenger: Hook heedance tracking relies on the agent explicitly self-reporting heeded state; if agents don't write the heedance record that may be intended behavior, not a broken pipeline — distinguish 'agent did not report' from 'report was lost' before concluding the pipeline is broken.

## Proposal 8/12 — abandoned-threads _→ applied 2026-07-28_

- Target:     `~/.claude/skills/deadline/SKILL.md`
- Intent:     File a concrete proposal for the deadline undeclared-slips gap — pinned 4 consecutive days without action
- Confidence: 0.62
- Rationale:  The ring/shed ledger catches declared cuts but has no mechanism for undeclared slips (items queued, believed in-progress, never built — surfacing only when the owner explicitly asks). This gap appeared in the pinned thread every day of the review week. The fix is a /deadline skill update: add a periodic in-flight confirmation pass requiring each active F-series item to have either a verified-built artifact or an explicit deferral in the shed log.
- Draft:
```
Add to /deadline SKILL.md: a verify-in-flight step that lists all active items from the current ring and requires each to have either a build-artifact pointer or an explicit NOT-BUILT deferral note. Items with neither are flagged as undeclared slips before the next ring closes.
```
- Challenger: The /deadline skill is user-directed; adding a mandatory confirmation pass changes the interaction contract without knowing whether the user wants that overhead during deadline sprints — may impose process where the user wants speed.

## Proposal 9/12 — abandoned-threads _→ rejected 2026-07-28_

- Target:     `~/.claude/skills/adversarial-review/SKILL.md`
- Intent:     Surface the 3 unanswered design questions from the adversarial-review skill debut pin for an explicit user decision before first real use
- Confidence: 0.55
- Rationale:  The adversarial-review skill (built 2026-07-21) has been pinned for 2 consecutive days with 3 specific unresolved design questions: (1) does the relevance gate filter direction without suppressing real findings? (2) does the claims-ledger + execute-the-gap design produce findings /skeptical-review structurally cannot? (3) what dispositions should first-run reviews apply? Early runs with unresolved design produce results that are hard to interpret as signal vs. noise.
- Draft:
```
No code change — flag for user decision on at least questions (1) and (3) before first real use on a PR; question (2) is better answered empirically after a run.
```
- Challenger: Design questions are best answered empirically by running the skill and observing dispositions, not by pre-deciding; blocking first use pending upfront design decisions delays the learning cycle unnecessarily.

## Proposal 10/12 — atone-analyst _→ applied 2026-07-28_

- Target:     `~/.claude/rules/testing.md`
- Intent:     Add [generated-artifact-scan] as a named tag: a generated artifact that passes type-check, lint, and tests may still have structural content gaps — verify the output against its referencing code
- Confidence: 0.60
- Rationale:  The affirm event refines exercise-based-verification for generated artifacts: 'Checked generated CSS against referencing JS before calling a deploy done; caught a prod-scan gap three green signals missed.' This is not covered by any existing named tag in testing.md. Generated files are the one surface where a green build provides the weakest correctness signal — the generator's output correctness is untested by the suite that validates the generator ran.
- Draft:
```
+ [generated-artifact-scan] After generating a CSS file, codegen output, migration artifact, or any file other code references by content: read the generated output against the referencing code before calling done. A green build proves the generation ran, not that the output is correct.
```
- Challenger: This is a narrow sub-case of [declared-ready] and [render-before-judge] already in testing.md; a third tag covering overlapping ground increases cognitive load without changing the underlying behavioral directive.

## Proposal 11/12 — graduation-curator _→ rejected 2026-07-28_

- Target:     `~/.claude/proposals.jsonl`
- Intent:     Pre-file a design-mocks-never-consulted rule stub proposal to be activated when the slug reaches 3x S3
- Confidence: 0.52
- Rationale:  design-mocks-never-consulted is at 2x S3 with a 'blind spot, slow down' annotation in the SessionStart hook. Given the 'still recurring this week' signal, it is likely to hit 3x within 1-2 sprints. Pre-filing a stub in proposals.jsonl now means the graduation step is a one-line activation rather than a full write under time pressure when the third event lands.
- Draft:
```
File via propose.sh: 'Pre-draft rule for design-mocks-never-consulted — activate at 3x S3. Content: before implementing any user-facing UI surface, grep for Figma/design mocks and consult them. Precheck: can you name the design document for this surface right now?'
```
- Challenger: Pre-drafting rules below the graduation threshold adds proposals.jsonl backlog noise; some proposals already sit 60+ days unreviewed, and a pre-draft may be superseded by the actual atone event when it lands.

## Proposal 12/12 — gcc-fitness-scorer _→ applied 2026-07-28_

- Target:     `~/.claude/scripts/hooks/declared-ready-stop.sh`
- Intent:     Audit why declared-ready-without-runtime-exercise remains at 12x S3 despite a mechanical gate — check whether the hook has precision gaps for specific claim patterns or is being bypassed
- Confidence: 0.64
- Rationale:  declared-ready-without-runtime-exercise is at 12x S3, equal in recurrence to structural-claim-without-reading-code which has no mechanical gate. The declared-ready-stop.sh hook should be blocking new instances. If events are still accumulating at 12x, either the hook misses specific claim patterns (e.g. sub-agent completion reports phrased as success), agents reroute around it by deferring the claim to a new turn, or there are intermittent mute-file events. Cross-referencing W30 hook fires against atone events in the same window identifies which.
- Draft:
```
Audit: cross-reference declared-ready-stop.sh fire log with atone declared-ready events in the W30 window. If atone events exist without a corresponding hook fire, the hook has a precision gap. If fires exist alongside atone events, the hook is firing but not preventing recurrence — revisit the signal wording or add a follow-up turn check.
```
- Challenger: The hook is loop-safe by design (fires once per claim per turn then steps aside). 12x S3 across the full account history may simply reflect the behavioral difficulty of the change, not a hook failure — the base rate before the hook existed would be the appropriate comparison point.

