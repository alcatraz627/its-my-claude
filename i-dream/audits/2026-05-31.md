# 2026-05-31 — L3 weekly audit

Surfaced: 25 · Approved: 0 · Applied: 0

## Proposal 1/25 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/comments.md`
- Intent:     Add a pre-write gate checklist for source-comment-hygiene
- Confidence: 0.82
- Rationale:  source-comment-hygiene is S3, 4x and appeared in the top-4 every single day this week. rules/comments.md exists but clearly isn't deterring the pattern. The rule needs a mechanical pre-write checklist: 'before writing any comment, answer: is this WHY (not WHAT)? does it contain a plan ref, phase tag, or TODO that has shipped? is it decorated (ascii, bullets for a 1-liner)?' — identical to how testing.md has the topic-tagged checklist.
- Draft:
```
## Pre-write gate
Before writing or editing any comment block, answer:
1. WHY not WHAT — does this explain a constraint, not restate the code?
2. No plan refs — no Phase/Track/Round/PR markers
3. No decoration — no ascii bullets, no emoji, no horizontal rules
4. Not already obvious — would a competent reader wonder 'why is this here?'
If any answer is 'no', rewrite or delete.
```
- Challenger: rules/comments.md is already detailed; adding a checklist may duplicate the existing bulleted rules rather than strengthen enforcement.

## Proposal 2/25 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/over-corrected-tuning-request.md`
- Intent:     Create new rule: distinguish 'soften a trigger' from 'disable the mechanism'
- Confidence: 0.62
- Rationale:  over-corrected-tuning-request-into-disable appeared Day 5 (S3). Pattern: user says 'this hook fires too aggressively' → agent disables the hook entirely rather than adjusting the threshold. The failure mode is treating a tuning request as a deletion request. No existing rule covers this. Confidence is moderate because it's 1x so far.
- Draft:
```
# Over-correcting a tuning request into a disable

When the user asks to 'soften', 'relax', 'reduce', or 'quiet' a hook/rule:
- DEFAULT: adjust the threshold, condition, or message — NOT the enabled state
- Only disable if the user explicitly says 'turn it off', 'remove it', 'disable it'

**Why:** A misfire rate of 20% is a calibration problem. A disable has blast radius = 100% miss rate.

**Diagnostic signal:** You changed `enabled: false`, deleted a hook file, or commented out a trigger — and the user said 'softer/less/reduce', not 'off/remove/delete'.
```
- Challenger: Single S3 event; may be too early to graduate to a standing rule before a recurrence confirms the pattern.

## Proposal 3/25 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/testing.md`
- Intent:     Promote declared-ready-without-runtime-exercise from topic-tag to full rule section
- Confidence: 0.77
- Rationale:  declared-ready-without-runtime-exercise is S3, 3x (Days 6 and 7 both show it). Currently it appears only as a `[declared-ready]` topic-tag bullet in testing.md — the same lightweight treatment as `[render-before-judge]`. At 3x S3, it deserves a dedicated section with a diagnostic signal, not just a bullet.
- Draft:
```
## Never announce done without running the code path

Before writing 'done', 'fixed', 'complete', or 'ready':
- Start the server / run the script / call the endpoint at least once
- Observe actual output, not just test results
- 'Tests pass' ≠ 'I ran it'

If the code path cannot be exercised (no server, restricted env), say so explicitly: 'I cannot verify this — please run X and confirm Y.' Never imply you verified something you did not run.

**Diagnostic signal:** You wrote a completion summary without a single tool call that exercised the changed code path.
```
- Challenger: testing.md topic-tag section already calls this out; adding a separate section risks the file becoming redundantly long with two entries for the same pattern.

## Proposal 4/25 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/sub-agent-outputs.md`
- Intent:     Add a quick 'pre-dispatch gate' checklist as a boxed callout at top of file
- Confidence: 0.68
- Rationale:  sub-agent-material-output-left-in-conversation-only is S3, 2x in Day 7's TLDR despite rules/sub-agent-outputs.md being a detailed document. The rule is long (the detail is deep in the file). Agents read depth only when triggered; a compact gate at the top of the file that fires before the dispatch is written would catch it earlier.
- Draft:
```
## ⚡ Pre-dispatch gate (30 seconds — do this BEFORE writing the Agent prompt)

- [ ] Is the output material? (cited later, user-facing, or >5 min of work)
- [ ] Have I assigned an absolute output path?
- [ ] Did I write 'write to <path> before returning' in the dispatch prompt?
- [ ] Did I add a `test -f <path>` verification step after the call?

If any box is unchecked → fix before dispatching.
```
- Challenger: The gate already exists in the body of the file; moving it to a boxed callout is cosmetic and doesn't change agent reading behavior if the agent already skips the file.

## Proposal 5/25 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/scripts/hooks/`
- Intent:     Propose a PreToolUse hook to block Write calls that look like they contain plan-ref comments
- Confidence: 0.60
- Rationale:  source-comment-hygiene 4x S3 — the pattern keeps recurring despite a detailed comments rule. A hook that pattern-matches `Phase [0-9]`, `Track [A-Z]`, `Round [0-9]`, `[claude@` in Write/Edit tool bodies and warns before saving would move enforcement from 'agent remembers' to 'system blocks'. Confidence 0.60 because false-positive rate on legitimate uses of those strings in non-comment contexts (test fixtures, user data) could be high.
- Draft:
```
scripts/hooks/guard-comment-hygiene.sh (new file)
PreToolUse on Write and Edit.
Pattern: rg '(Phase [0-9]|Track [A-Z]|Round [0-9]|\[claude@|Pre-fix:|Post-fix:)' in the tool body.
If match: emit warning with matched line, prompt for confirmation. Non-blocking (warn, not hard-stop) because legitimate strings exist in non-comment contexts.
```
- Challenger: Regex-based comment hygiene hooks have high false-positive rates on test fixtures, documentation strings, and user-authored content — may create more friction than it prevents.

## Proposal 6/25 — affirm-analyst _→ skipped or rejected_

- Target:     `~/.claude/subconscious/dreams/`
- Intent:     Flag: affirm system has zero events across all 7 days — pipeline is not being fed
- Confidence: 0.72
- Rationale:  Every day this week, affirm shows 'no activity tracked yet'. The affirm/events.jsonl mechanism is described in CLAUDE.md as a counterweight to atone, but if it never receives events, the system is asymmetric: mistakes accumulate, successes are never encoded. Either the user is not triggering /affirm after good sessions, or the infra isn't collecting. This is an observation, not a GCC rule change — but it warrants a proposal to add a session-end nudge.
- Draft:
```
Add to CLAUDE.md Tier 1 'Post-session insight' bullet: 'If the session had a confirmed-good non-obvious choice (user said yes/perfect/exactly to an unusual call) — invoke /affirm before closing, not just /core-dump.'
```
- Challenger: CLAUDE.md already mentions affirm under 'Atone' section as a planned counterweight; adding another mention may just create noise before the affirm system is actually operational.

## Proposal 7/25 — affirm-analyst _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     Add affirm as a standard post-session ritual step alongside /core-dump
- Confidence: 0.55
- Rationale:  The session-end ritual currently mentions /core-dump and post-session insight but affirm is described as 'planned' with an incomplete pipeline. Given zero affirm events for 7 days, the ritual is not surfacing it. Even with a partial affirm pipeline, capturing affirm events would build the backlog for when the system ships.
- Draft:
```
In 'Post-session insight' section, append: 'If any non-obvious choice was validated this session (user confirmed an unusual approach, accepted an edge case call): run `/affirm` with the choice slug before closing. Affirm events backfill the counterweight even if the derived view hasn't shipped.'
```
- Challenger: If the affirm system infrastructure is not yet operational, adding ritual steps for it creates debt — agents will attempt /affirm and hit incomplete tooling.

## Proposal 8/25 — dreams-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/grep-scope-before-claiming-absence.md`
- Intent:     Add 'scope-and-authorization' cross-link to the existing infra-before-grep rule
- Confidence: 0.65
- Rationale:  Day 5 cross-domain association (conf 0.88): grep-scope-before-claiming-absence and scope-and-authorization-discipline share the root of 'premature commitment before state is verified'. The existing rule covers grep scope but doesn't connect to the authorization/scope-ceiling angle. Adding a Related section link and a diagnostic signal for the authorization variant would unify both failure modes under one rule.
- Draft:
```
In rules/grep-scope-before-claiming-absence.md ## Related section, add:
- `rules/communication.md` § Scope Control — the authorization twin: claiming a file doesn't exist and editing outside declared scope are both premature commitments.

Add diagnostic signal: 'You are about to edit a file not named in the user's task AND you have not grepped the full tree to confirm it's the right location.'
```
- Challenger: Cross-linking two distinct failure modes under one rule risks diluting both; scope-authorization lives in communication.md already and doesn't need a second home.

## Proposal 9/25 — dreams-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/testing.md`
- Intent:     Add 'assume-before-verify' as a unified topic tag that consolidates structural-claim, infra-before-grep, and helper-return-type patterns
- Confidence: 0.70
- Rationale:  Day 5 and Day 7 cross-domain associations both flag the same meta-pattern: the agent acts on an assumption it could cheaply verify. Three separate rules (structural-claim-without-reading-code, grep-scope-before-claiming-absence, helper-return-type-assumption) all describe the same underlying failure. Adding a single `[assume-before-verify]` topic tag in testing.md that points to all three would give a single precheck surface for the cluster.
- Draft:
```
Add to testing.md topic-tagged rules section:
- `[assume-before-verify]` — Before acting on any claim about what exists, what a function returns, or how a subsystem works: verify it. The three sub-rules are: (1) grep full tree before claiming absence, (2) read file:line before asserting architecture, (3) grep helper def before chaining methods on its return value. If you feel certain without having checked — that certainty is the trigger.
```
- Challenger: Each of the three rules already has its own file with a diagnostic signal; a meta-tag in testing.md adds a fourth mention without reducing the maintenance surface.

## Proposal 10/25 — dreams-analyst _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     Add proactive risk-surface reminder to the 'Terse in = terse out' Tier 0 rule
- Confidence: 0.63
- Rationale:  Day 5 and Day 7 cross-domain association (conf 0.88): the assume-before-verify failure mode and proactive-risk-flagging reward are two sides of the same gap. CLAUDE.md's Tier 0 section mentions 'Scope = ceiling' and 'State = ephemeral' but doesn't have a positive framing: 'when you feel the urge to act without checking, surface the risk instead.' Adding one sentence to the existing Tier 0 block would give this cluster a top-of-context nudge.
- Draft:
```
In CLAUDE.md Tier 0 'Terse in = terse out' block, add after 'Treat all state as ephemeral':
'When you feel certain about state you haven't read — surface the uncertainty as a risk, don't act on the assumption. Proactive flagging is cheaper than a correction round-trip.'
```
- Challenger: CLAUDE.md Tier 0 is already dense; one more sentence risks pushing readers to skim past the section entirely.

## Proposal 11/25 — gcc-fitness-scorer _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     Extract the Atone system description from Tier 0 into a Tier 1 features pointer
- Confidence: 0.68
- Rationale:  The 'Atone — mistake tracking & affirmation system' block in CLAUDE.md Tier 0 is ~20 lines of operational detail (script commands, file paths, hinter behavior). By the placement heuristic (>15 lines → sub-file, 80%-skip → Tier 2+), this should be a Tier 1 brief+pointer. The detail is already in features/atone-system.md or similar — check before moving. Keeping it inline in Tier 0 inflates the always-loaded context.
- Draft:
```
Replace the full atone block in Tier 0 with:
'**Atone system** — mistake-pattern log at `~/.claude/atone/events.jsonl`, curated view at `~/.claude/mistake-patterns.md`. Record mistakes via `/atone`; inspect via `atone.sh list/search/show`. Full guide: `features/atone-system.md`.' (3 lines instead of 20)
```
- Challenger: The atone system is high-frequency enough that agents benefit from the full operational detail in Tier 0 rather than having to load a sub-file every session.

## Proposal 12/25 — gcc-fitness-scorer _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     The Tier 0 'Session ID' rule generates IDs that appear unused in practice — evaluate for demotion
- Confidence: 0.57
- Rationale:  The Session ID rule ('generate [keyword]-[keyword]-[2hex]') is Tier 0 (always-loaded, always-executed) but session IDs appear only in WAL headers and checkpoint files — both of which are ephemeral artifacts. If the WAL or checkpoints are not being actively used for session recovery, the Session ID rule has become overhead. Propose an audit: check whether WAL and checkpoint files reference session IDs and whether those IDs are ever used for lookup. If not, demote to Tier 2.
- Draft:
```
Run: `rg '[a-z]+-[a-z]+-[0-9a-f]{2}' ~/.claude/ -l` to check where session IDs actually appear. If only in wal.jsonl and _checkpoint files (not in any user-facing lookup), move the rule from Tier 0 inline to a Tier 2 pointer in the features/context-retention.md link.
```
- Challenger: Session IDs are a lightweight habit that costs <1 second per session and anchors WAL entries; demoting them on a 'might not be used' hypothesis risks breaking the checkpoint/recovery flow silently.

## Proposal 13/25 — gcc-fitness-scorer _→ skipped or rejected_

- Target:     `~/.claude/rules/`
- Intent:     infra-before-grep (5x S3) has a rule, yet the pattern persists — the rule may need a MANDATORY pre-edit precheck added
- Confidence: 0.73
- Rationale:  infra-before-grep is the highest-recurrence atone pattern this week (5x S3). rules/grep-scope-before-claiming-absence.md exists and is detailed. The persistence suggests the rule is being read but not internalized as a gate. The structural-claim rule has the same shape. Both rules would benefit from a boxed '## Before you type the next sentence' precheck at the top — mirroring how rules/comments.md has a pre-write gate.
- Draft:
```
Add to rules/grep-scope-before-claiming-absence.md at top:
## Precheck (fires before any 'no existing X' sentence)
1. Did I grep the full project root (not just one subdirectory)?
2. Did I use at least 2 keyword variants (synonym, abbreviation, concept term)?
3. If both greps return empty — am I about to propose new infrastructure?
If (3) yes and (1) or (2) no — run the wider grep before continuing.
```
- Challenger: The rule already has a 'Concrete pattern' section with good/bad grep examples; adding a precheck box duplicates that information in a different format.

## Proposal 14/25 — gcc-fitness-scorer _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     The 'MANDATORY quick-rules' section has grown to 6 items — evaluate against the placement heuristic
- Confidence: 0.60
- Rationale:  CLAUDE.md's 'MANDATORY quick-rules' section has 6 bullet rules with varying detail. By placement rules (>3 lines → consider sub-file), some of these (the nested-path rule, HTML dark-mode rule) are 2-3 lines of inline detail that should move to Tier 1 pointer entries pointing to conventions/ files. The section is load-bearing as a never-miss bar, but individual items can be condensed to 1-line summaries with file pointers.
- Draft:
```
For each MANDATORY rule that exceeds 1 line: condense to '[Rule name] — [one sentence]. See [conventions/file.md].'
Example: 'Every HTML output needs a dark/light toggle. Default dark, CSS vars only. → conventions/html-output.md'
```
- Challenger: The 'MANDATORY quick-rules' section exists precisely to be readable without loading sub-files; condensing to pointers defeats the purpose of the never-miss bar.

## Proposal 15/25 — graduation-curator _→ skipped or rejected_

- Target:     `~/.claude/rules/source-comment-hygiene.md`
- Intent:     Graduate source-comment-hygiene to its own rules/*.md file with a diagnostic signal and hook proposal
- Confidence: 0.78
- Rationale:  source-comment-hygiene is S3, 4x — the same recurrence count that graduated generalize-before-enumerate (3x S3) and structural-claim-without-reading-code (4x S3) to their own rules files. rules/comments.md covers the topic but is a style guide, not a behavioral rule with a diagnostic signal. A dedicated rules/source-comment-hygiene.md with the graduated pattern format (issue / cause / fix / precheck / diagnostic-signal) would match how other high-recurrence patterns were handled.
- Draft:
```
New file: rules/source-comment-hygiene.md
---
name: source-comment-hygiene
tier: 1
category: rules
---
# Source comment hygiene
Comments in source must be terse, durable, and human-first. No plan refs, no decoration, no TODOs that shipped.

## Precheck
Before writing any comment: (1) WHY not WHAT? (2) No Phase/Track/Round/PR markers? (3) No `[claude@` tags mixed with human text?

## Diagnostic signal
You are about to write a comment that contains a phase reference, a timeline note, a 'this was added for X' attribution, or an ascii decoration. That is the pattern.
```
- Challenger: rules/comments.md is already a comprehensive rule file covering this exact content; creating a second file for the same topic creates maintenance split.

## Proposal 16/25 — graduation-curator _→ skipped or rejected_

- Target:     `~/.claude/scripts/hooks/guard-infra-before-grep.sh`
- Intent:     Propose a PreToolUse hook that fires when Write/Edit creates a new module file without a prior Grep call in the same turn
- Confidence: 0.65
- Rationale:  infra-before-grep 5x S3, rule exists, pattern persists. The rule is documentation-level. A lightweight hook that checks 'did the last N tool calls in this turn include a Grep on the full project root?' before allowing a new file Write would make the guard mechanical rather than advisory. This mirrors how guard-anthropic-credentials.sh prevents a known blast-radius pattern at the tool layer.
- Draft:
```
scripts/hooks/guard-infra-before-grep.sh (new, PreToolUse on Write)
Trigger condition: Write is creating a new file (not editing existing), AND tool call log for this turn contains no Grep with path == project root.
Action: warn 'You are creating a new file without a full-tree existence check. Run: rg -n "<concept>" <project-root>/ before creating.' Non-blocking warn, not hard-stop.
```
- Challenger: A turn-level tool-call log is not reliably available to hooks; the implementation may require harness-level support that doesn't exist yet, making this proposal speculative.

## Proposal 17/25 — graduation-curator _→ skipped or rejected_

- Target:     `~/.claude/rules/testing.md`
- Intent:     Add declared-ready-without-runtime-exercise as a full second-tier section, not just a topic tag
- Confidence: 0.80
- Rationale:  declared-ready-without-runtime-exercise is S3, 3x over Days 6 and 7. The topic-tag entry in testing.md says '[declared-ready] — Never announce done without running the code path at least once.' That's 15 words. At 3x S3, this deserves a full section with a definition of 'code path exercised', acceptable substitutes when running is blocked, and the exact phrasing that triggers the rule ('done', 'fixed', 'complete', 'ready', 'works').
- Draft:
```
Add after the 'UI/frontend verification' section in testing.md:
## Declaring done requires runtime exercise
Before writing 'done', 'fixed', 'complete', 'ready', or 'works':
- At minimum: one tool call that caused the changed code path to execute (server start + curl, script run, test with real data)
- Acceptable substitutes when blocked: explicit statement — 'I cannot run this in this env; please verify by running X'
- NOT acceptable: 'tests pass', 'the logic looks correct', 'this should work'

**Trigger phrases:** 'done', 'fixed', 'complete', 'ready to ship', 'should work', 'this works'
**Precheck:** Did any tool call after my last edit cause this code path to run?
```
- Challenger: testing.md already has the topic-tag covering this; a second section makes the rule appear twice in the same file, which may reduce clarity rather than increase it.

## Proposal 18/25 — graduation-curator _→ skipped or rejected_

- Target:     `~/.claude/rules/env-var-config-pattern.md`
- Intent:     Add a precheck gate to the existing env-var-config-pattern rule to match the 2x S3 recurrence
- Confidence: 0.72
- Rationale:  adding-env-var-reads-without-checking-the-project-s-config-pattern is S3, 2x (appeared Days 6 and 7). rules/env-var-config-pattern.md exists and has the bash rg command, but the rule doesn't have a precheck that fires before the read is written. At 2x S3, adding a precheck to the existing file (rather than creating a new one) is the proportionate response.
- Draft:
```
Add to rules/env-var-config-pattern.md after the bash example:
## Precheck (before writing any env var read)
1. Run: `rg -n 'process\.env\.|os\.getenv|os\.environ' src/` (or project root)
2. If results show a config module or schema — route through it, don't add a raw read
3. If results are scattered raw reads — follow that pattern but file a proposal to centralize

**Diagnostic signal:** You are about to write `process.env.X` or `os.getenv('X')` and you have not run the project-pattern grep.
```
- Challenger: The rule already contains the rg command; the precheck is just a restatement in checklist format — marginal improvement for the file size increase.

## Proposal 19/25 — abandoned-threads _→ skipped or rejected_

- Target:     `~/.claude/subconscious/dreams/`
- Intent:     Flag: i-dream domain pipeline has zero entries across all 7 domains for 7 consecutive days
- Confidence: 0.85
- Rationale:  Every domain (introspection, intuition, metacog, memory, pinned, sessions, weekly_briefing, claude-audit) shows 'no activity tracked yet' every day this week. This is either a pipeline bug (ingestion isn't running), a data gap (no sessions are being tracked), or a display bug (the digest isn't reading the correct paths). This is the second week of zero cross-domain activity except for the manually crafted associations in Days 5 and 7. Action: check whether i-dream ingest is running and whether session data is being written to the intake directories.
- Draft:
```
Audit action (not a GCC change): run `ls -lt ~/.claude/subconscious/dreams/ingest-queue/` and `ls -lt ~/.claude/subconscious/dreams/traces/` to confirm data is being written. If empty, check whether the ingest cron is live with `launchctl list | rg alcatraz`.
```
- Challenger: This is an operational observation, not a GCC proposal; the output format requests GCC-targeted changes and this falls outside scope.

## Proposal 20/25 — abandoned-threads _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     generalize-before-enumerate has been S3, 3x for 5+ days without a hook — the rule exists but enforcement hasn't moved past documentation
- Confidence: 0.70
- Rationale:  generalize-before-enumerate was S3, 3x in Days 1-4 and its rule file exists (rules/generalize-before-enumerate.md). However, unlike speculative-abstractions (which has a clear precheck: 'does the caller exist NOW?'), generalize-before-enumerate has no precheck and no hook. At 3x S3, the absence of enforcement suggests the rule isn't being applied as a gate. Propose adding a precheck to the rule file that mirrors the speculative-abstractions precheck format.
- Draft:
```
Add to rules/generalize-before-enumerate.md:
## Precheck (before writing any multi-case helper or switch)
1. List the actual cases you found by grepping — not the cases you expect
2. If your list has 1 item, inline it. If 2+ items, verify each exists with a file:line citation
3. If you cannot enumerate the cases from grep results, you cannot write the generalization

**Diagnostic signal:** You are writing a function with argument categories, switch branches, or union types — and your case list came from intuition, not grep output.
```
- Challenger: The existing rule already says 'enumerate the actual cases first'; a precheck reformulates the same instruction without adding new information.

## Proposal 21/25 — abandoned-threads _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     The 'render before saving artifact files' Tier 0 rule has no associated hook — a PostToolUse hook on Write for .md/.html files would make it mechanical
- Confidence: 0.65
- Rationale:  The render-before-saving rule (graduated S2, 4x) is Tier 0 inline in CLAUDE.md. The rule says 'render-check your own output with glow file.md'. At 4x recurrence, the pattern persists despite Tier 0 placement. A PostToolUse hook on Write that detects .md/.html output and emits a reminder — 'run: glow <path> to verify' — would make the check visible at the moment it's needed rather than relying on the agent to remember.
- Draft:
```
scripts/hooks/remind-render-check.sh (new, PostToolUse on Write)
Trigger: Write call where file_path ends in .md or .html
Action: emit to stderr 'Render-check: glow <file_path> (or bat -l md <file_path>) — confirm no gum-rendered ASCII, broken H1, or missing frontmatter'
Non-blocking. One line. Mutable via touch ~/.claude/.render-check-off.
```
- Challenger: A PostToolUse reminder on every .md Write would fire dozens of times per session on routine file operations, creating hook fatigue that causes agents to tune out all hook output.

## Proposal 22/25 — challenger _→ skipped or rejected_

- Target:     `~/.claude/rules/`
- Intent:     Meta-challenge: most proposals this week are adding MORE rules to patterns that ALREADY HAVE rules
- Confidence: 0.80
- Rationale:  infra-before-grep has a rule (5x S3 still). structural-claim-without-reading-code has a rule (4x S3 still). generalize-before-enumerate has a rule (3x S3 still). source-comment-hygiene has comments.md. The pattern is: adding a rule doesn't stop recurrence. Before adding yet more rules, the audit should ask: why are existing rules not working? Candidates: (a) rules files are not being loaded in-context, (b) the diagnostic signals aren't firing at the right moment, (c) the agent reads the rule but the impulse to skip fires faster. The right intervention may be hooks (mechanical), not more text (advisory).
- Draft:
```
GCC structural proposal: add a 'Rule efficacy review' step to the weekly audit prompt. For any rule that has 2+ atone recurrences after the rule was written, flag it as 'rule exists but not deterring' and require the proposal to be a hook (not a rule text addition) before it can pass.
```
- Challenger: This meta-challenge is itself speculative — the recurrence data doesn't distinguish 'rule written but not loaded' from 'rule loaded but ignored'; both require different fixes.

## Proposal 23/25 — challenger _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     Challenge: the Tier 0 section is too long for 'always-loaded' to be a meaningful claim
- Confidence: 0.75
- Rationale:  CLAUDE.md Tier 0 is ~120 lines of dense rules. If every agent loads this, the context cost per session is non-trivial. More importantly, at 120 lines, agents skim rather than internalize. The claimed 'always-load' guarantee becomes a fiction past the first 30 lines. Propose measuring actual agent behavior: how many of the Tier 0 rules show up as correctly-applied behaviors vs. as atone recurrences? Rules that appear in atone recurrences despite Tier 0 placement are evidence of the skimming problem.
- Draft:
```
Audit step: `rg -n 'structural-claim|infra-before-grep|source-comment|generalize-before' ~/.claude/CLAUDE.md` — if these are mentioned or linked in Tier 0 but still recurring, the Tier 0 guarantee is not holding for these specific patterns. Consider moving them to hooks instead of keeping them as Tier 0 text.
```
- Challenger: 120 lines of Tier 0 is dense but not unusual for complex agent configuration; the alternative (moving rules to Tier 1/2) reduces load guarantee which may worsen recurrence.

## Proposal 24/25 — graduation-curator _→ skipped or rejected_

- Target:     `~/.claude/rules/ambiguous-file-action-halt.md`
- Intent:     Add the parallel Read+Write trap to the CLAUDE.md Tier 0 quick-rules as a one-liner
- Confidence: 0.68
- Rationale:  rules/ambiguous-file-action-halt.md was graduated on 2026-05-31 (today's date per context). The 'parallel Read+Write trap' described in that file — batching Read and Write in the same tool block doesn't fix the 'file not read' block — is a subtle tooling behavior that agents frequently get wrong. Given today's graduation, adding a one-liner to the MANDATORY quick-rules section would give it Tier 0 visibility during the initial hot period before the rule becomes well-internalized.
- Draft:
```
Add to CLAUDE.md MANDATORY quick-rules:
'**Write to an unfamiliar file path requires a prior Read in a separate turn.** Batching Read+Write in the same tool block is a race — the Write fires blind. Read first, then Write in the next turn.'
```
- Challenger: The rule was just written today; promoting to Tier 0 before it has any recurrence data is premature — let it stabilize in Tier 1 first.

## Proposal 25/25 — dreams-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/sub-agent-outputs.md`
- Intent:     Add the 'render-check your own output' section as a standalone precheck in the pre-dispatch gate
- Confidence: 0.63
- Rationale:  sub-agent-material-output-left-in-conversation-only is S3, 2x in Day 7. The sub-agent-outputs rule already has a 'render-check your own output' section buried at the bottom. The cross-domain pattern from the week suggests this step is being skipped. Moving the render-check into the pre-dispatch gate checklist (4 bullets at top of file) would ensure it's encountered before dispatch rather than after the artifact is already written.
- Draft:
```
Add 5th bullet to pre-dispatch gate:
- [ ] After the sub-agent returns and writes the file: run `test -f <path> && glow <path>` (or `jq . <path>` for JSON) — confirm the file renders correctly before linking it into the context doc.
```
- Challenger: The render-check is a post-dispatch step, not a pre-dispatch step; mixing it into the pre-dispatch gate makes the gate misleadingly named and harder to follow sequentially.

