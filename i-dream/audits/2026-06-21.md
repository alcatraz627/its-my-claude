# 2026-06-21 — L3 weekly audit

Surfaced: 21 · Approved: 0 · Applied: 0

## Proposal 1/21 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/scripts/hooks/rg-replace-guard.sh`
- Intent:     Narrow rg-replace hook to match actual rg invocations, not patterns that mention rg flags as string content
- Confidence: 0.90
- Rationale:  The rg-replace hook fired during this audit when I ran 'bash ... | rg "structural-claim|rg -rn|..."' — it matched 'rg -rn' inside my grep PATTERN, not in an actual rg invocation with -rn flags. This is the cross-cutting false-positive class pinned for 7 consecutive days: the hook scans the raw command string for dangerous tokens, so any quoted pattern string containing those tokens triggers it. The mist-20260612-110132-40 RCA explicitly recommends 'Possible hook: PreToolUse Bash regex on rg -[a-zA-Z]*r warning that rg -r is --replace'. That hook exists but is matching at the wrong granularity.
- Draft:
```
In rg-replace-guard.sh: restrict trigger to cases where 'rg' appears as a command token (following a pipe, semicolon, or string start) AND the NEXT non-whitespace token begins with '-' AND that flag cluster includes 'r'. Do NOT fire when 'rg' appears inside a quoted string argument to another command. Test: 'cmd | rg "rg -rni search" file' must NOT fire; 'rg -rni pattern file' MUST fire.
```
- Challenger: Shell command parsing via regex has well-known edge cases (nested quoting, variable expansion, process substitution). The fix may need Python's shlex rather than another regex layer to be reliable.

## Proposal 2/21 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     Add 'rg -r is --replace, not recursive' as a Tier-0 one-liner in Shell safety
- Confidence: 0.88
- Rationale:  rg -r (--replace) caused 4-5 atone events within a single session cluster (2026-06-12 x4: phantom mnidata symbol recorded into facts registry). The RCA on mist-20260612-110132-40 explicitly recommends a hook — that hook exists but is itself causing false positives. While the hook is being fixed, a Tier-0 one-liner ensures every agent sees this at session start regardless of hook state. 'Never use rg -r' is a near-absolute rule since recursive search is rg's default and -r is virtually never needed for that purpose.
- Draft:
```
Add to CLAUDE.md Tier-0 § 'Shell safety': '**`rg -r` is `--replace`, not recursive** — rg recurses by default; `-r` silently rewrites output (rg -rni eta → metadata becomes mnidata). Never use combined flags like `-rn`, `-ri`, `-rni`. Correct flags: `rg --no-ignore --hidden "PATTERN" path/`.'
```
- Challenger: Tier-0 already points to rules/shell.md where this could live; adding another one-liner risks Tier-0 bloat for content that belongs in the shell rules detail file.

## Proposal 3/21 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/structural-claim-without-reading-code.md`
- Intent:     Add absence-claims explicitly to the precheck gate and cross-reference infra-before-grep as the sibling rule
- Confidence: 0.80
- Rationale:  structural-claim-without-reading-code has 10x S3. The rule's precheck covers authority/presence claims ('X is the authority on Y'). But the 2026-06-18 event ('Claimed zod was in package.json + recommended new dependency') and 2026-06-19 event ('Asserted rate-limits resets_at field') are absence/content claims — not covered by the current trigger-phrase list. Adding 'does not exist', 'is not installed', 'is not in package.json' to the trigger set and cross-linking infra-before-grep unifies the two families under one precheck.
- Draft:
```
Add to rules/structural-claim-without-reading-code.md § 'The rule' trigger list: '- "X does not exist" / "there is no Y" / "X is not in Z" — absence claim, same precheck applies'. Add: 'For infrastructure-level absence claims (no module/helper exists), see the sibling rule: grep-scope-before-claiming-absence.md'. Add reciprocal cross-link in grep-scope rule.
```
- Challenger: Adding absence claims to a rule named 'structural-claim-without-reading-code' stretches the rule's semantic scope; the two rules cover genuinely different decision moments and cross-links are sufficient without merging trigger lists.

## Proposal 4/21 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/rewrite-scope-inventory.md`
- Intent:     New rule: before rewriting a component, inventory its current user-visible behaviors as a preservation checklist
- Confidence: 0.72
- Rationale:  slug 'dropped-existing-feature-during-rewrite' (2026-06-17, S2): agent silently dropped the activity-ribbon feature during a component rewrite. The failure: rewriting from the spec/intent rather than from the current implementation. This is the Chesterton Fence pattern applied to behavioral features: the rewriter didn't know the feature existed and therefore didn't preserve it. The existing chesterton-fence atone (2026-05-11) covers structural splits; this covers behavioral features within a single component and generalizes beyond the specific incident.
- Draft:
```
New file rules/rewrite-scope-inventory.md: Before rewriting any component the user names as a rewrite target, read the current implementation and list: what user-visible behaviors does this component currently implement? That list is a PRESERVATION CHECKLIST. The rewrite must preserve all items unless the user explicitly says to drop one. A 'clean rewrite from spec' that skips this step will silently drop features the spec never mentioned.
```
- Challenger: This adds a mandatory read step to every rewrite, which may be overhead for small components or truly greenfield rewrites. Consider scoping the trigger: 'rewrite targets with >50 lines or user-named features'.

## Proposal 5/21 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/python-import-propagation.md`
- Intent:     New rule: before propagating a Python import pattern to a new site, check the module-level convention and project-wide dominant style
- Confidence: 0.65
- Rationale:  slug 'propagating-legacy-inline-imports-in-python-backend' has 2 events (2026-05-29, 2026-06-15). Both incidents: agent saw a lazy/inline import at one call site, treated it as the convention, and propagated it across N other sites — bypassing the module-level import style. The first site's style was a legacy exception, not the rule. rules/env-var-config-pattern.md covers the same 'check convention before adding' class for env vars; Python imports need the same rule.
- Draft:
```
New file rules/python-import-propagation.md: Before adding an import at a new Python site, check (1) how this module imports the same package at its own top, and (2) rg the project-wide dominant pattern. If the existing call site you're modeling used a lazy import, verify whether that was intentional (circular dep, optional dep, startup perf) — not just stylistic — before treating it as the convention to follow. Default to module-level imports unless a documented reason exists.
```
- Challenger: rules/env-var-config-pattern.md already covers the 'check convention before adding' class; this is a 2-event pattern that could be a subsection there rather than a new file.

## Proposal 6/21 — atone-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/testing.md`
- Intent:     Add orphaned-process cleanup as a named topic tag [process-cleanup] with the kill-verify pattern
- Confidence: 0.62
- Rationale:  slug 'orphaned-background-test-processes' (2026-06-18, S2): 18 orphaned 'yes' load-generator processes left after a test. This is the second process-cleanup event (first: ghost-test-server-processes-from-port-reuse, 2026-05-02). rules/shell.md says no run_in_background unless asked, but test processes may be started via subprocess.Popen or direct shell backgrounding — a different surface. The existing [topic-tagged rules] section in testing.md is the right home for this, consistent with how other repeat patterns are documented there.
- Draft:
```
Add to rules/testing.md § 'Topic-tagged rules': '- `[process-cleanup]` — Any process launched during a test (load generators, servers, mock daemons) must be tracked and terminated before declaring test done. Pattern: launch → run → assert → KILL → verify-kill (pgrep -f <name> → zero matches). Missing the last two steps is the orphaned-process failure. Check for survivors before declaring results valid.'
```
- Challenger: Most test frameworks handle this automatically via fixtures; the rule may be too low-level for a global config and better expressed as a project-local pattern in each project's conftest.py or test harness.

## Proposal 7/21 — affirm-analyst _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     Promote Cluster F (audit-before-action) from Tier-2 pointer to Tier-1 brief with the key invariant stated inline
- Confidence: 0.68
- Rationale:  Cluster F (audit-file-character-before-applying-global-rule) has 3 affirm events (2026-05-15 x2, 2026-05-29) — the strongest repeated positive signal in the affirm corpus. The behavior: agent reads the destination file's character before moving code into it or applying a global rule, surfaces any tension, and hands the decision back. Currently only a Tier-2 pointer in the Conventions table. Placement.md 'silent failure bump' criterion: agents applying rules blindly to the wrong file context is a silent failure — 3 affirm events catching it justify Tier-1 visibility.
- Draft:
```
Add to CLAUDE.md Tier-1 (Core mechanisms) table: a new row for rules/audit-file-character-before-applying-global-rule.md with brief: 'Before applying a global rule to a specific file, read its top ~20 lines — files have a character (types-only, hooks-only) and a rule that's right in general can be wrong here. Surface the tension rather than silently applying or silently skipping.' Triggers: topic:refactor, topic:consolidate, move-into-file operations.
```
- Challenger: Tier-1 is for mechanisms that 'activate most sessions'; this rule fires only when a global rule is about to be applied to an unusual file — it's conditional enough to stay Tier-2 with a trigger tag rather than Tier-1 always-visible.

## Proposal 8/21 — affirm-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/scope-convention-discipline.md`
- Intent:     New rule: handle a single-consumer exception locally rather than bending the project-wide convention
- Confidence: 0.65
- Rationale:  affirm slug 'scope-convention-change-to-one-consumer' (2026-06-08, cluster G): agent scoped an S3 path-convention change to the single consumer that needed it rather than changing the project-wide convention. This is a positive catch that should become a standing rule. It is the inverse of speculative-abstractions applied to conventions: bending an existing convention for one consumer has project-wide blast radius. The 'local exception over convention change' heuristic is not encoded anywhere in the GCC.
- Draft:
```
New file rules/scope-convention-discipline.md: When one consumer needs behavior that diverges from a project-wide convention (naming, import style, path format), implement the exception locally at that consumer — do not change the convention. Precheck: 'How many existing callers use this convention?' If >1, any change to the convention affects them all; scope the exception instead. If only one caller exists currently, local exception is still the safer default unless the convention itself is wrong, which requires a separate explicit discussion.
```
- Challenger: This rule requires knowing how many callers a convention has — which itself requires a full-tree grep, potentially triggering the infra-before-grep failure class if done narrowly. The precheck may introduce its own verification burden.

## Proposal 9/21 — affirm-analyst _→ skipped or rejected_

- Target:     `~/.claude/rules/prescribed-flattery-as-fix-for-pushback.md`
- Intent:     Add positive framing counterpart: encode 'intelligent-disobedience-over-sycophancy' as the affirmed behavior this rule enables
- Confidence: 0.60
- Rationale:  'intelligent-disobedience-over-sycophancy' has 2 affirm events (2026-05-17, 2026-05-26): agent pushed back on a user assumption with evidence rather than capitulating. This is the positive counterpart to the existing rule's 'don't prescribe flattery' mandate. Currently the rule only names what NOT to do. Adding the affirmed positive behavior ('when you have evidence, argue the case') gives agents a model to emulate, not just a pattern to avoid. Both affirm events say 'argued the case with reasoning' or 'chased actual cause instead of accepting wrong user framing'.
- Draft:
```
Add to rules/prescribed-flattery-as-fix-for-pushback.md § 'What this rule does NOT mean': 'The opposite of prescribed flattery is NOT silence or hedging — it is evidence-backed disagreement. When you have a file:line, a measurement, or a documented counter-example: state it plainly. "The rule says X, but this file's character is Y because..." That directness is the behavior this rule is trying to enable. Affirm slug: intelligent-disobedience-over-sycophancy.'
```
- Challenger: rules/communication.md § 'escape hatch' already covers evidence-backed disagreement; this addition risks duplicating that guidance at a different location rather than extending it.

## Proposal 10/21 — dreams-analyst _→ skipped or rejected_

- Target:     `~/.claude/scripts/hooks/rg-replace-guard.sh`
- Intent:     URGENT (7-day pin confirmed live this session): fix the raw-command-string-scanning false positive — the hook fires on pattern text containing rg flags, not on actual rg flag invocations
- Confidence: 0.95
- Rationale:  Pinned in all 7 daily digests. Confirmed live: during this audit I ran 'bash ... | rg "structural-claim|rg -rn|infra-before-grep|..."' — the hook fired because 'rg -rn' appeared inside my grep PATTERN argument, not as actual rg flags. The hook cannot distinguish 'rg PATTERN_CONTAINING_DASH_R file' from 'rg -r REPLACEMENT_STRING file'. This is not theoretical — it interrupted a real audit query. 7 days of pinning without action means the hook has been breaking real work for at least a week.
- Draft:
```
Immediate action: (1) Read the rg-replace-guard hook source. (2) Add test: 'bash -c "ls | rg \"rg -rni search\""' must NOT fire. (3) Narrow match: only fire when rg is the command (argv[0] or post-pipe token) AND the flag cluster immediately following is the problematic one. (4) Test: 'rg -rni actual_pattern src/' MUST fire. Ship before next session.
```
- Challenger: Without read access to the hook source during this audit (hooks directory was blocked by CWD restrictions), the fix description is structural guidance rather than a concrete diff — actual implementation may differ.

## Proposal 11/21 — dreams-analyst _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     Add 'verify before claiming' as a Tier-0 bridge sentence spanning structural-claim and infra-before-grep
- Confidence: 0.72
- Rationale:  Day 7 cross-domain association: assume-before-verify (atone, 10x + 6x) ↔ proactive-risk-surface (affirm/memory). Both top patterns trace to acting on unverified assumptions. They have separate rules at Tier-2 but no unified Tier-0 entry. A single Tier-0 sentence ('before asserting how code works or that something doesn't exist, name the file:line or run the grep') would be visible at session start and would prime the agent's verification instinct before the pattern fires.
- Draft:
```
Add to CLAUDE.md Tier-0 § 'State = ephemeral': 'Before asserting how code works (authority, returns, exists or doesn't), name the file:line or run the grep. Unverified claims are the source of structural-claim-without-reading-code (10x S3) and infra-before-grep (6x S3) — both trace to acted-on assumptions with no verification step.'
```
- Challenger: This is already covered by the two dedicated Tier-2 rules with detailed precheck gates; a vague Tier-0 one-liner may be less useful than hooks that fire at the actual decision moment.

## Proposal 12/21 — dreams-analyst _→ skipped or rejected_

- Target:     `~/.claude/features/context-retention.md`
- Intent:     Add burst-session guidance: when iterative same-day sessions are detected, lower checkpoint threshold and frontload 'last experiment + next step'
- Confidence: 0.57
- Rationale:  Sessions signal (7 consecutive days): burst-clustering with up to 5 sessions in 2 hours. Burst sessions are iterative debugging where the user /clears between attempts. Current checkpoint rule triggers at tool #30 — but burst sessions may /clear after 10-15 tool calls. The checkpoint misses entirely, and the next session starts cold on the exact context that most needs continuity. A burst-mode heuristic: when the WAL shows a prior session with a similar topic within 2 hours, lower the checkpoint threshold and make the checkpoint frontload 'what just failed and what to try next' rather than background context.
- Draft:
```
Add to features/context-retention.md § 'Checkpoint timing': '**Burst sessions** (same-topic /clear cycles within ~2h): lower /core-dump trigger to tool #15. Prioritize in the checkpoint: (1) last hypothesis tested and result, (2) what the result rules out, (3) single most likely next step. Omit background context — the user just /cleared, they know the project; they need the experiment state preserved across the /clear boundary.'
```
- Challenger: Burst-mode detection via WAL session timestamps is noisy — multiple independent sessions on different topics within 2h would incorrectly trigger burst mode. Simpler: document this as a user-invoked mode rather than auto-detecting it.

## Proposal 13/21 — gcc-fitness-scorer _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     Add structural-claim and infra-before-grep as Tier-0 one-liners — both have been Tier-2 for months with 10x and 6x S3 showing the tier is not binding agents in-flight
- Confidence: 0.78
- Rationale:  structural-claim (10x S3) and infra-before-grep (6x S3) have dedicated Tier-2 rules. Recurrence rates have not decreased — the latest events for both are from this week (2026-06-18 and 2026-06-15). Per skill-spec-update-not-honored: rules that are not in the agent's loaded context at decision time are advisory. Tier-2 is only loaded on trigger; both patterns fire before the trigger would load the rule. Tier-0 one-liners are visible at session start and would prime the precheck instinct before either pattern can fire.
- Draft:
```
Add two one-liners to CLAUDE.md Tier-0 § 'Shell safety' or 'State = ephemeral': (1) 'Before creating new infrastructure (helper, module, route), grep the FULL project tree first — not just the obvious directory. (6x S3 infra-before-grep)' (2) 'Before asserting a symbol/package/authority exists or doesn't, name the file:line or run rg -F. A zero-match grep means re-examine the command, not record an absence. (10x S3 structural-claim)'
```
- Challenger: Tier-0 already has 13 inline blocks. Adding two more one-liners risks the section becoming unreadable. The right fix is hooks that fire at the decision moment, not more advisory text at any tier — per the challenger's meta-proposal.

## Proposal 14/21 — gcc-fitness-scorer _→ skipped or rejected_

- Target:     `~/.claude/rules/hook-authoring-discipline.md`
- Intent:     New rule: hooks must include a documented false-positive test case and must match command-token position, not raw string substring
- Confidence: 0.73
- Rationale:  Both the rg-replace hook and the cli-gating pattern (6 claude-audit events this week) fire on documentation prose and pattern strings, not on actual dangerous commands. The root cause is consistent: regex scanning the raw command string for a dangerous token anywhere in it. A hook-authoring rule requiring (1) a false-positive test case in the header comment and (2) argv-position matching rather than substring scanning would prevent this class at authoring time.
- Draft:
```
New file rules/hook-authoring-discipline.md: Every PreToolUse hook script must include in its header: # INTENDED TRIGGER: <one-line>, # FALSE POSITIVE TEST: <concrete example that superficially matches but must NOT fire>, # LAST TESTED: <date>. Pattern rule: match dangerous tokens at command-token position (rg as argv[0], flag as the next token) — never via substring scan of the full raw input string. Before shipping a hook, run it against: a command where the guarded token appears only inside a quoted argument. If it fires: narrow before shipping.
```
- Challenger: Documentation requirements on hook scripts create authoring friction. A test harness (scripts that run hooks against known inputs and assert outcomes) would catch false positives without relying on author discipline.

## Proposal 15/21 — gcc-fitness-scorer _→ skipped or rejected_

- Target:     `~/.claude/CLAUDE.md`
- Intent:     Demote 'Signal session state via tab title' from Tier-0 inline to Tier-1 feature entry — it fires only in Ghostty sessions with specific intent
- Confidence: 0.55
- Rationale:  Tier-0 has grown to 13 inline blocks. 'Signal session state via tab title' is conditional on using Ghostty and having a clear session intent — it's not a universal session behavior. Per Placement.md: '80%-skip → Tier-2+'. The tab-title feature already has a full guide at features/tab-title.md. Moving the inline block to a Tier-1 Features table row with trigger 'tool:tab-title, topic:ghostty' reduces Tier-0 length without losing the rule — it becomes loadable on demand.
- Draft:
```
Remove the 'Signal session state via tab title' section from CLAUDE.md Tier-0. Add to Tier-1 Features table row: '| features/tab-title.md | tool:tab-title, topic:ghostty, topic:terminal-title | Ghostty tab-title slots: status(✅⚠️❌) · mode(auto-derived) · intent(session noun) · focus(sub-task). Use tab-title.sh to convey state glanceably. |' The Tier-0 quick-rules retain the glyph-perm and ssh lines as one-liners if needed.
```
- Challenger: This rule is currently Tier-0 precisely because it was being missed; demoting it risks the same silent omission that got it promoted. The Tier-0 cost is one paragraph that saves confusion across every Ghostty session.

## Proposal 16/21 — graduation-curator _→ skipped or rejected_

- Target:     `~/.claude/scripts/hooks/infra-before-grep-nudge.sh`
- Intent:     Graduate infra-before-grep from advisory rule to PreToolUse hook — 6x S3 with existing rule since April 2026 shows the rule is not binding agents at decision time
- Confidence: 0.63
- Rationale:  infra-before-grep (6x S3) has rules/grep-scope-before-claiming-absence.md since at least April 2026. Newest event: 2026-06-15. The rule has not reduced recurrence. This is the same trajectory as declared-ready before the Stop hook was added. A PreToolUse Write hook that fires on new-file creation under module directories would inject a nudge at the exact moment of risk. Exit 0 (advisory, doesn't block). The declared-ready precedent shows mechanical nudges at the decision moment work where advisory text doesn't.
- Draft:
```
New hook scripts/hooks/infra-before-grep-nudge.sh (PreToolUse on Write): trigger when file_path matches 'src/|lib/|backend/|frontend/|api/' AND the file does not already exist. Emit additionalContext: '⚡ infra-before-grep (6x S3): before creating this module, grep the full project tree to verify this problem isn't already solved. rg --no-ignore "<concept>" <root>/. See rules/grep-scope-before-claiming-absence.md'. Add to settings.json preToolUse.
```
- Challenger: New-file creation is very frequent; this hook fires on every new React component, new test file, and new API route — creating hook fatigue that makes the signal invisible on the rare case it's actually warning about duplicate infra.

## Proposal 17/21 — graduation-curator _→ skipped or rejected_

- Target:     `~/.claude/scripts/hooks/declared-ready-stop.sh`
- Intent:     Add collect-not-run exclusions: pytest --collect-only and tsc --noEmit must not count as 'run signals'
- Confidence: 0.68
- Rationale:  declared-ready-without-runtime-exercise has 5x S3 and a Stop hook. rules/testing.md explicitly tags [collect-not-run] as a known failure mode. The 2026-05-29 'TS-build-as-done collapsed a URL-contract refactor' event is a clear tsc --noEmit case. If the hook counts any Bash invocation involving pytest or tsc as a 'run signal', it has a systematic false-negative for collect-not-run. Adding an exclusion list for these specific invocation patterns would close that gap.
- Draft:
```
In declared-ready-stop.sh: in the 'run signal detected' logic, add an exclusion set — these invocations are COLLECT, not RUN: 'pytest.*--collect-only', 'pytest.*--co\b', 'tsc.*--noEmit', 'npm run.*type-check' (often maps to tsc --noEmit). Comment: '# [collect-not-run] these exercise zero assertions — do not count as run evidence'. Only invocations that actually execute test assertions count as run signals.
```
- Challenger: The hook may not have access to per-turn Bash command history — it may only see a boolean 'did Bash run this turn'. If so, adding exclusions requires instrumenting the hook with command logging, which is a larger architectural change.

## Proposal 18/21 — graduation-curator _→ skipped or rejected_

- Target:     `~/.claude/rules/grep-scope-before-claiming-absence.md`
- Intent:     Add package.json dependency-check as a named example and cross-reference structural-claim for the single-file absence case
- Confidence: 0.70
- Rationale:  The 2026-06-18 structural-claim event ('Claimed zod was in package.json + recommended new dependency') bridges both rules: it's an absence claim about a specific file (structural-claim territory) AND led to recommending new infra (infra-before-grep territory). Currently neither rule names package.json dependency-checking as an example. Adding it to grep-scope makes the failure mode concrete, and cross-linking structural-claim closes the gap between module-level and file-level absence claims.
- Draft:
```
Add to rules/grep-scope-before-claiming-absence.md § 'Concrete pattern': 'For package.json dependency absence claims: before "X package is not installed, add it", run: jq ".dependencies[\"X\"], .devDependencies[\"X\"]" package.json. Zero output from both means it's absent. Do NOT assert absence from a pattern search — the package name may appear in comments or lock files. Cross-reference: rules/structural-claim-without-reading-code.md (sibling rule for single-file content claims).'
```
- Challenger: This adds a very specific example (package.json) to a general rule about module-level grep scope — the specificity may make the rule feel less broadly applicable to other absence-claim scenarios.

## Proposal 19/21 — abandoned-threads _→ skipped or rejected_

- Target:     `~/.claude/scripts/hooks/`
- Intent:     URGENT: rg-replace false-positive class has been pinned 7 consecutive days and was confirmed live during this audit — this is an active daily blocker, not a backlog item
- Confidence: 0.93
- Rationale:  Every daily digest June 14-20 carries the same pinned signal about hook false positives on documentation text. This session confirmed it: the rg-replace hook fired on a legitimate audit query whose grep PATTERN happened to contain 'rg -rn' as a search string. The hook has been unmodified for at least 7 days while generating daily interference. The cli-gating pattern (6 claude-audit events) is likely the same root cause applied to a different hook. Neither has been actioned. These are production hooks that are actively degrading the quality of agent work every day they remain unfixed.
- Draft:
```
Prioritize this week: (1) Read all hook scripts matching 'rg' and 'cli' patterns. (2) For each: add a false-positive test case to its header. (3) Narrow the match from raw-string substring to command-token position. (4) Re-test the known false positives (read-only gh calls, HEREDOC writes, 'rg "pattern containing rg flags"'). The 7-day pin is this audit system telling you this is the highest-priority fix in the GCC.
```
- Challenger: The hooks directory was inaccessible during this audit (CWD restriction). The proposal cannot be verified against actual hook source and may describe a fix that doesn't match the real implementation.

## Proposal 20/21 — abandoned-threads _→ skipped or rejected_

- Target:     `~/.claude/subconscious/`
- Intent:     Flag: per-domain detail sections have been empty for 6 of 7 days while top-signals reference those same domains — possible i-dream digest assembly gap
- Confidence: 0.65
- Rationale:  Days 1-6 daily digests: every per-domain summary section (affirm, atone, claude-audit, etc.) shows '_(no activity tracked yet)_' while the top-signals section references live data from those same domains ('All 6 events in this delta share a single slug...'). Day 7 partially resolves (cross-domain associations appear). This structural inconsistency — top-level summary contains data, per-domain detail is empty — suggests either a minimum-event threshold is hiding content or the per-domain aggregation step is not running for most digest runs.
- Draft:
```
Investigate i-dream digest assembly: check whether per-domain sections have a minimum-event threshold that's suppressing content. If the digest's per-domain section shows data only when events exceed N (where N > 0), verify the threshold is intentional. If the assembly step is not running, debug 'i-dream daily --debug'. This is an i-dream pipeline bug, not a GCC change — file as a bug in i-dream's issue tracker or proposals.jsonl.
```
- Challenger: This may be intentional: per-domain sections may show only new delta events not already in the TLDR. If TLDRs pull from a running aggregate and per-domain sections are delta-only, 'no new events' is correct — not a bug.

## Proposal 21/21 — challenger _→ skipped or rejected_

- Target:     `~/.claude/rules/`
- Intent:     Meta-challenge: structural-claim (10x), infra-before-grep (6x), and declared-ready (5x) each have dedicated rules yet keep recurring — adding more rules is not the fix; mechanical enforcement is
- Confidence: 0.85
- Rationale:  All three top-recurrence patterns have well-written rules with explicit precheck gates, diagnostic signals, and named failure modes. Recurrence has not decreased. The most likely explanation: Tier-2 rules are not consulted at the decision moment because they only load on trigger, and the trigger fires after the mistake. The effective interventions in this account's history are hooks (declared-ready-stop reduced that pattern after the hook shipped) and Tier-0 one-liners (visible at every session start). The proposals in this audit lean heavily toward new rule files — those will have the same binding rate as the existing ones: low.
- Draft:
```
Evaluate before writing new rule files: (1) Would this pattern be caught by a hook that fires at the specific decision moment? If yes, build the hook instead of the rule. (2) Is the failure happening because the agent didn't load the rule, or because the rule exists and the agent didn't follow it? If the former: Tier-0 one-liner. If the latter: no amount of text will help — the fix is structural (better task planning, plan→review cycle). This week's best-ROI changes are: fix the rg-replace hook false positive, add infra-before-grep and structural-claim as Tier-0 one-liners, and tighten declared-ready-stop for collect-not-run. New standalone rule files for new 1-2x patterns should wait for 3+ recurrences.
```
- Challenger: This meta-challenge assumes hooks and Tier-0 text are categorically more effective than rules — but declared-ready was also a Tier-0-equivalent warning for months before the hook, suggesting hook enforcement requires the rule to have existed first.

