<!-- i-dream project brief · 2026-05-13T12:34:35.008339+00:00 · 6 patterns / 0 insights -->
## What this project is about
General-purpose developer workspace (`/private/tmp`); no fixed domain. Work style is terse, autonomous, and scope-controlled.

## Things to do (or keep doing)
- Push back on incorrect user statements factually — the user explicitly rewards accuracy over appeasement
- Opt-in by default: new behavioral features on shared/generic components must be disabled unless the consumer explicitly enables them
- Match response length to input; short directives mean execute, not discuss

## Things to avoid
- Don't commit or push without fresh explicit approval for that specific operation — prior approval does not carry forward
- Don't branch on error message strings (regex, `.includes`, `.startsWith`) — use stable structured error codes
- Don't touch files or directories outside the declared in-scope folder, even when closely related
- Don't pull in new test libraries without first checking existing test patterns in the codebase

## Open questions / known gaps
- No recurring project-specific frustrations detected yet; patterns here are all cross-project discipline rules, not domain tensions
