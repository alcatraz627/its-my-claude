<!-- i-dream project brief · 2026-08-21T23:39:21.038479+00:00 · 6 patterns / 0 insights -->
## What this project is about
A technical workspace (likely architecture/planning focused) where the dominant working style is high-autonomy execution with strong emphasis on structured, visually scannable output over prose.

## Things to do (or keep doing)
- Always include tables, JSON payload shape examples, and ASCII architecture diagrams in any technical planning doc — prose-only output is a defect
- Keep going when the user says "keep going" or invokes /atone for a prior stop — treat it as hard authorization to proceed without re-raising the same concern
- Use markdown over HTML by default; if HTML is chosen, the light/dark toggle is mandatory, no exceptions

## Things to avoid
- Don't halt mid-task unless the blocker is genuine and only the user can resolve it — soft uncertainty is not a valid stop reason
- Don't use literary or narrative phrasing ("The build chase ended somewhere useful") in any technical output — same class as em-dash violations
- Don't re-raise a concern the user already dismissed with "keep going" or an explicit continuation signal

## Open questions / known gaps
- All patterns are single-occurrence (1×) — no multi-session signal yet; treat these as provisional until reinforced
