<!-- i-dream project brief · 2026-07-14T03:24:33.126281+00:00 · 13 patterns / 3 insights -->
## What this project is about
Ghostty terminal theme tooling — likely a CLI/script-based project for managing, previewing, or distributing themes. Working style is iterative with strong emphasis on live exercise over test coverage claims.

## Things to do (or keep doing)
- Always run the affected code path live before claiming done — test coverage passing is not verification
- Dispatch an adversarial reviewer sub-agent immediately after implementing any complex feature
- Surface the systemic observation behind every local workaround ("this inline fix suggests X is missing from the system")
- Push back with evidence when a design decision seems wrong — the user expects debate, not capitulation

## Things to avoid
- Don't treat social reassurance ("I trust you", "that's fine") as authorization to remove safeguards or gates — it's comfort, not a removal mandate
- Don't use zero-defaults (`dict.get('x', 0)`) when source data may be absent — fabricated plausible values suppress investigation
- Don't commit or push without explicit fresh approval per the protected-repos policy; prepare the diff and hand it off
- Don't turn off a noisy behavior entirely when the user says it's "too aggressive" — tune intensity down, not to zero

## Open questions / known gaps
- Task lists drift from reality across long editing sessions; reconcile completed vs open items before stopping or switching context
- Inline styling workarounds recur, suggesting the UI kit (if any) has coverage gaps that haven't been formally addressed
