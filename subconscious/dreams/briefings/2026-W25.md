# Weekly Briefing — 2026-W25 (2026-06-21)

_Generated 2026-06-21 09:00 +05:30_

## Cross-domain patterns

The dream pass found unusually dense cross-domain signal this week — leading with it.

- **`ai-smell-prose` recurred intra-session after the graduation rule went live** (`atone` ↔ `pinned/prop-20260619-194931-ba`). The rule is Tier-0. The pattern still fired twice in the same conversation the day it was promoted. Advisory graduation is confirmed insufficient; `prop-20260619-194931-ba`'s Stop hook is not a nice-to-have, it's overdue.

- **Late-session drift co-fires across unrelated rule classes** (`structural-claim-without-reading-code` ↔ `ai-smell-prose`, both within 2 minutes of the same session). These aren't separate failures — they're a single verification-discipline collapse that happens to surface on whichever active rule has the nearest trigger. A co-fire detector (two independent slug violations in a short session window → escalated block) would catch this class that single-slug hinters miss.

- **`in-context-rule-not-applied-as-pre-action-gate` ↔ `pin-20260529152121-e6`**: The June-19 gate-miss and the Versable hinter-failure are the same failure shape — relevant rule fires as advisory, agent doesn't enforce it as a blocking pre-action check, pattern continues. The system has now reproduced this twice across unrelated rule classes.

- **Hook scanner false positives share a root with `self-permitting-exception-to-an-adr-hard-rule`**: The `cli-gating` hook blocking HEREDOC writes and read-only `gh` calls is a "good enough for now" substring match that was never stress-tested against realistic inputs. Same reasoning pattern that graduations warn against. The shared command-position-aware parser needs to explicitly prohibit naive substring matching in its header.

- **`declared-ready-without-runtime-exercise` applies to hook refactors too**: When the safe-delete / guard-system-dir-writes scanner is fixed, the refactor isn't done until each hook is exercised against one real false-positive (RCA prose containing `rm`) and one real true-positive. This needs to be in the refactor checklist, not assumed.

---

## What you worked on

Heavy week across two parallel Versable tracks plus active `.claude` infrastructure maintenance. The Versable sessions centered on documentation quality and product-facing prose. The `.claude` work was largely hook behavior — both fixing existing hooks and graduating new rules from the atone corpus. `local-models`, `better-file-browser`, and `sys-monitor` each got attention but at lower depth.

---

## What improved

- **Research-to-action translation**: The week surfaced a clean positive pattern — when exploration produces findings, there's now awareness that a separate explicit step is needed to translate them into product-and-behavior terms. That gap being named is progress.
- **Graduation velocity**: The `ai-smell-prose-against-stored-voice` S3 event moved from atone → RCA → `rules/audience-aware-writing.md` → Tier-0 in CLAUDE.md within the same week it fired. That's the fastest the graduation arc has run.
- **Cluster F (Audit-before-action)** has 3 affirm events over 14 days — the audit-before-applying-a-global-rule habit is sticking in places where it previously wasn't.

---

## Recurring frustration

AI-smell prose. All five documentation-tone corrections this week are variants of the same failure: em-dashes, label:fragment rows, promotional "why this matters" framing, internal uncertainty leaking into user-facing copy, overcorrection into warm narrative when the em-dashes get pulled. The rule exists at Tier-0. The RCA exists. The pattern fired anyway — twice in the same session. The frustration is real and the source is clear: there's no mechanical gate. Every instance this week was caught by the user after the fact.

---

## Worth examining

- **`prop-20260619-194931-ba`** — the Stop-stage prose enforcement hook. Cross-domain analysis assigns it higher priority than its current "open" status reflects. Ship it.
- **`cli-gating` hook** — 6 events this delta, all high-impact false positives. Blocking HEREDOC writes and read-only `gh` calls. Needs the command-position-aware parser before it blocks something load-bearing in a session where the user isn't watching.
- **WAL pattern deduplication** — the promoted insights section has 7 near-identical entries about the markdown→JSONL migration. The extraction pipeline is over-indexing on structural changes. Worth a quick dedup pass before the noise crowds out real signal.
- **Intra-session co-fire detection** — the `structural-claim` + `ai-smell` co-fire within 2 minutes suggests a verification-collapse mode that single-slug hinters don't catch. Candidate for a new hinter class.

---

## One idea

Ship `prop-20260619-194931-ba` (the prose Stop hook) this week — write it, test it against a PR description session, and close the proposal. The Tier-0 rule entry is live and proven insufficient; the mechanical gate is the only thing left to try, and every week without it is another round of the same correction.

---

## One question

If a rule fires as advisory twice in the same session and doesn't change behavior, what would it take to make you trust the mechanical gate more than the rule text?
