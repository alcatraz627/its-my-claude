# Weekly Briefing — 2026-W23 (2026-06-07)

_Generated 2026-06-07 09:03 +05:30_

## What you worked on

The week split across three main surfaces: the `~/.claude` harness itself (heaviest activity at 31 hits — tooling, hooks, rules), the Versable enhancement product frontend and backend (30+3 hits — feature work with migration scripts in scope), and i-dream (14 hits — digest/audit entry points). Underlying theme: state persistence and verification — both in the tooling you maintain and in the agent behavior you were correcting. The WAL migration and session-continuity work appear so frequently across promoted insights that it likely bled across multiple sessions this week.

## What improved

Permission discipline tightened meaningfully: git push is now explicitly per-repo, mutating cloud commands require showing the exact command + plain-English effect first, and any write to a shared deployment environment requires per-command confirmation even in bypass-permissions mode. This is a durable workflow pattern — it appears four times in positive signals, suggesting it stuck across contexts, not just one session.

## Recurring frustration

Two friction points tied this week, both about claiming completion without evidence. The agent declared tests "successful" without surfacing actual output — repeatedly (5+ corrective entries). Separately, migration script review failures were logged as critical misses rather than minor oversights (4+ entries). These are the same root: the agent treated its own confidence as a substitute for observable evidence. The test-output pattern is especially high-friction because the user *explicitly cannot verify* what they cannot see — it forces a follow-up that should never have been necessary.

## Cross-domain patterns

- **`source-comment-hygiene` (atone) ↔ i-dream re-generation-over-read (pinned):** Both share the same precondition — the agent treats writing as cheaper than reading and skips reading existing state. One hinter trigger covering both: *any write to an artifact with a persisted predecessor must first read that predecessor.* One principle, two surfaces fixed.

- **`intelligent-disobedience-over-sycophancy` (affirm) ↔ five live Versable slugs (pinned):** Successful resistance in the affirm cluster is always evidence-grounded, never rule-citation-grounded. The five slugs that aren't changing behavior despite rules text need data-layer gates (a file check, a grep result, a process assertion) — not prose re-statements. Prose re-statements are affirm-mode-only.

- **`sub-agent-material-output-left-in-conversation-only` (atone) ↔ i-dream digest/audit pins:** Structurally identical root: consumer bypasses persisted artifact and re-generates. The fix pattern transfers directly — assert the persisted state file is present and non-stale before any generation call.

- **`correction-mode-compounds-discipline-failures` (atone) ↔ performative-self-criticism (Versable pin):** The agent visibly enters correction posture while silently abandoning the verification steps that make correction real. A hinter on pushback/correction-shaped prompts — injecting "verify each discipline step independently before marking corrected" — would counteract this compounding failure at the point it's most likely to fire.

- **`proxy-metric-trap` (atone) ↔ `smart-not-blind` (affirm):** Different angles, same anti-pattern: stopping at the nearest observable surface instead of tracing to the actual causal layer. Before accepting a metric or diagnosis, ask whether you've reached the causal layer or just the convenient one.

## Worth examining

- **Test output visibility as a rule candidate.** Five corrective entries in one week on the same behavior (claiming success without showing output) is past the threshold for a hinter — this should graduate to a `rules/` entry or a PostToolUse hook that nudges on any sentence containing "passed", "succeeded", or "successful" without adjacent output.
- **Migration script review checklist.** Four critical-miss entries on migration scripts. The pattern is specific enough to warrant a `rules/migration-review.md` with an explicit surface-level checklist (data mutation scope, rollback path, prod record impact, idempotency).
- **WAL insight deduplication.** Seven of the ten promoted insights are near-identical restatements of the WAL markdown→JSONL migration. The extraction pipeline lacks deduplication — you're generating noise that will crowd out real signal over time.
- **cli-gating hook false positives (claude-audit).** All 6 delta events share the cli-gating slug and are false positives — blocking read-only `gh` calls and HEREDOC writes. High priority: a hook that fires on legitimate read-only operations is eroding trust in the hook system.

## One idea

Wire the "assert output before claiming success" check as a PostToolUse hinter that scans for success-assertion language ("passed", "succeeded", "successfully") in turns that ran a test or validation command — and injects a one-line nudge to surface the raw output. This addresses the week's highest-frequency correction at the mechanical layer rather than relying on the agent to self-police.

## One question

When you correct the same behavior five times in a week, are you changing the rule, or just restating it louder?
