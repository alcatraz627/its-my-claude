{
  "id": "bf4a7004-085b-4462-bcdd-83d0f78f3b13",
  "ts": "2026-07-13T22:32:11Z",
  "session_id": "jegs-catch-4c",
  "project_root": "/Users/alcatraz627/Code/Versable/staging-enhancement-product",
  "checkpoint_path": "/Users/alcatraz627/Code/Versable/staging-enhancement-product/_20260714-jegs-catch-4c.claude.md",
  "insights": {
    "worked": [],
    "didnt_work": [],
    "gotchas": [],
    "notes": [],
    "feedback": []
  },
  "pending": [
    "**#16 (URGENT, do not commit until done)** \u2014 Rebuild `_reconcile_years`. It is net-harmful: (a) withholding returns `[]` \u2192 `_derive_fits_select` yields `\"N\"`, a **fabricated negative claim**, and `fitment_kind` degrades to `\"application\"` (not `unknown`) so the application branch **still writes a fitment-shaped name**, making the `content_error` text a lie; (b) `_make_is_supported` blocks `Chevrolet` when research says \"Chevy\", blocks `make=\"N/A\"` (which the extractor's own prompt sanctions), and **leaks** `Ram` \u2282 Du**ram**ax / `Mini` \u2282 **mini**van; (c) the year check false-positives on ordinary research prose (exclusion clauses, supersession notes) and, since research is S3-cached, that withhold is **permanent**; (d) the `cache_bust` retry **writes its rejected result back into the shared cache**, so from run 2 the two passes are no longer independent. **Rebuild:** compare two extractor passes to each other, not to the prose.",
    "**#17** \u2014 Per-row Meta_Title budget as *guidance* (never a `max_length` shrink \u2014 that's a pre-rejection the user explicitly ruled out) + propagate the real validation failure reason (`EnhancementAgentV2.py:646` TODO; `validation_history` at `:304`/`:404` holds the actual errors) into `Content_Error`.",
    "**#13 (USER)** \u2014 PM/JEGS answers on the 6 spec conflicts in `QUESTIONS-FOR-PM.md`. The two that bite: the **254-char Short_Description cap** collides with the PM's \"we can go over 6 bullets\"; and making the quantity bullet conditional puts us in violation of **JEGS's own Quality guide check #12** (\"Quantity is the last bullet\"), so their reviewer will bounce our correct rows.",
    "**Lab #5** \u2014 violation cards nest `<li>` inside `<li>` \u2192 hydration error on every panel with a violation.",
    "**Lab #6** \u2014 back-nav to a URL with no `tab` param leaves the tab stuck.",
    "**Fixture F7** \u2014 the brace guard covers rule args only; a brace in a *template* or *step arg* crashes identically and is unguarded.",
    "**Fixture F9** \u2014 an empty-but-present column turns `should_not_contain(\"<<Brand>>\")` into `should_not_contain(\"\")`, failing every row forever with a message naming no column.",
    "Part-type classifier (deferred, low value): the Dorman batch's Part Types match **zero** of JEGS's 8,668-entry taxonomy, but the real 556-row batch is clean Title Case, and even normalized none of the 15 SKUs route to Wheel/Tire/Kit."
  ],
  "tags": []
}