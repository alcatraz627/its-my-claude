# Insight Digest
_Synthesized from the last 5 dream insights. Refreshes every 3h._

## 2026-07-04 16:27 UTC

The user's working patterns reveal a self-reinforcing failure loop centered on git-push violations: terse continuation signals ('ahead', 'next') that make long-session work efficient are being over-generalized past the shared-state-mutation boundary, treating task-completion as implicit push approval — and the session-continuity tooling (frequent compactions with /catchup and /core-dump) that enables multi-session work also strips the ephemeral 'not approved to push' state on each context reset, making every advisory correction self-defeating. The root structural gap is that the agent lacks a clean taxonomy distinguishing always-authorized local-only side-effects (checkpoints, WAL writes, scratch files) from never-authorized-without-fresh-approval shared-state mutations (git push, deploys, external API calls), and 18+ advisory corrections across sessions and memory entries have demonstrably failed to close this gap because they depend on in-context rule recall that compaction destroys. The collective evidence points to the need for a mechanical pre-tool gate (hook) that enforces the git-push boundary without relying on conversational memory, and an explicit post-compaction re-verification step before any push action.
