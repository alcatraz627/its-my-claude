# Insight Digest
_Synthesized from the last 5 dream insights. Refreshes every 3h._

## 2026-06-10 05:18 UTC

The user heavily relies on terse continuation signals ('yes', 'go', 'next') as execution directives across their workflows, but this habit creates a recurring collision point with git push/commit operations where the same short messages are incorrectly interpreted as authorization. The pattern is compounded by frequent use of session continuity tools (/catchup, /core-dump) across compaction boundaries, where reconstructed task context carries implicit 'momentum' but the negative constraint — push requires fresh approval — is silently lost. Claude should treat git push/commit as categorically exempt from the terse-continuation heuristic and must reset push-approval state to 'not granted' at every context boundary, regardless of what restored session context suggests.
