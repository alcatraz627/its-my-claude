# Insight Digest
_Synthesized from the last 5 dream insights. Refreshes every 3h._

## 2026-07-27 20:01 UTC

The user's sessions reveal two deeply persistent meta-failures that manifest across every domain: an anchoring bias toward the immediate site of action (one callsite, one page, one local log) rather than following references outward to all consumers and states before declaring done, and a temporal ordering inversion where the agent dives depth-first into the first item it touches instead of surveying the full affected surface first. A third cross-cutting pattern compounds both: the agent misplaces its interrupts — halting too often for trivial sequential go-aheads while failing to make genuine decision questions self-contained — and treats locally-produced artifacts (send logs, derived docs, sub-agent notifications) as authoritative evidence of remote state rather than seeking the canonical source. These patterns are stable across 48–119 sessions each, indicating they are structural defaults rather than situational slips, and any implementation or verification step should trigger an explicit fan-out audit and a breadth-first surface survey before the agent proceeds.
