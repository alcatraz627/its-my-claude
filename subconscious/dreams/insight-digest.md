# Insight Digest
_Synthesized from the last 5 dream insights. Refreshes every 3h._

## 2026-07-15 18:47 UTC

The user's sessions reveal a single structural failure mode expressed consistently across coordination state, task tracking, config validity, and access control: writes that are mechanically necessary for correctness (TaskUpdate calls, IPC replies, git commits, phase checkpoints) get deferred as 'bookkeeping' while work continues on stale assumptions, and systems that encounter unknown or missing input emit plausible-looking defaults (zeros, ALLOWs, inferred contracts) instead of failing explicitly — both patterns compound because a stale ledger makes the next deferral more likely and a plausible output suppresses investigation. A secondary pattern reinforces this: behavioral constraints placed at the advisory layer (spec text, skill docs) are bypassable by any agent that doesn't read them at startup, meaning the blast radius of the constraint is always smaller than the blast radius of the resource it protects. Claude should treat every state-ledger write as a blocking obligation that executes immediately after completing a unit of work, re-verify any state an external writer could have mutated between plan-time and action-time, and prefer explicit failures over plausible defaults when input is unknown.
