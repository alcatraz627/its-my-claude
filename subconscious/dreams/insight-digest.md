# Insight Digest
_Synthesized from the last 5 dream insights. Refreshes every 3h._

## 2026-08-16 11:31 UTC

The user consistently expects a breadth-first enumeration pass before any instance-level work, and the agent's structural depth-first default is the root cause of repeated scope-miss corrections across UI, pagination, and shared-component tasks. A second systemic pattern compounds this: the agent treats high-confidence inferences about visual artifacts and structural state as facts, skipping the source lookup that would catch mismatches — a failure that is self-concealing precisely because the inference feels complete. Across session boundaries, both problems compound through poor ephemeral-state persistence and silent infrastructure stalls, meaning the user disproportionately bears the cost of re-establishing context that the agent should have written to a durable surface the moment it was known.
