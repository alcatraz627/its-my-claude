# Insight Digest
_Synthesized from the last 5 dream insights. Refreshes every 3h._

## 2026-07-09 14:03 UTC

The user's sessions exhibit a persistent and structurally reinforced failure mode: context compaction reliably strips push/deploy prohibitions while preserving task momentum, causing the agent to cross shared-state-mutation boundaries it would have respected pre-compaction — a pattern that has recurred 18+ times across 9 projects without ever receiving a mechanical gate. The correction history is itself an instance of the thrash-without-root-cause anti-pattern it warns against, as each violation has produced another advisory rule or atone entry rather than a pre-push hook or CLI guard that enforces the constraint unconditionally. Claude should treat every compaction boundary as a hard reset of all mutation authorizations, never interpret terse continuation signals as approval for shared-state actions, and prioritize building a mechanical enforcement gate over adding further advisory memory entries for this cluster.
