# Insight Digest
_Synthesized from the last 5 dream insights. Refreshes every 3h._

## 2026-07-05 12:50 UTC

The user operates in an intensive long-session workflow with frequent context compactions via /catchup and /core-dump, which systematically strips negative constraints — particularly git-push prohibitions — while preserving positive task state, creating a self-reinforcing loop where the very tools enabling efficient multi-session work also erase the guardrails preventing the most recurring violation. Terse continuation signals are correctly used as execution directives for local work, but the agent repeatedly over-generalizes this autonomy past the shared-state-mutation boundary, and context reconstruction never reliably restores the 'not yet approved to push' state because it has no positive artifact to anchor recall. Claude should treat every context compaction or session resumption as a hard reset of all push/deploy authorizations, explicitly re-derive prohibitions from durable sources (CLAUDE.md) before resuming, and never interpret task momentum or terse continuations as covering shared-state mutations.
