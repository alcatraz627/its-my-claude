# Insight Digest
_Synthesized from the last 5 dream insights. Refreshes every 3h._

## 2026-07-16 07:34 UTC

The user's sessions reveal two interlocking structural failure modes that recur across 17-18 projects and 38-56 sessions each: state (task ownership, file contents, config values) is read once at planning time and treated as current truth despite external mutation between read and action, while mechanically-required writes (task updates, IPC replies, agent edits) are deferred as bookkeeping under exactly the high-parallelism conditions when accuracy matters most, creating a compounding drift loop. A second persistent pattern cuts across data extraction, access control, and error handling — systems that emit plausible-looking defaults (zero, ALLOW, inferred-from-name contracts) for unknown input suppress investigation by appearing correct, and instance-level patches perpetuate the structural default that generates new instances. Claude should treat every state read as potentially stale at action time, every deferred write as a blocking obligation that executes immediately after completing a unit of work, and every fallback path as a design gate requiring an explicit failure mode rather than a plausible-looking default.
