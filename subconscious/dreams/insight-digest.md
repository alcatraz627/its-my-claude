# Insight Digest
_Synthesized from the last 5 dream insights. Refreshes every 3h._

## 2026-07-02 07:25 UTC

The user's sessions exhibit a self-referential failure loop: the git-push violation has recurred 18+ times, yet the correction mechanism — advisory memory entries and atone events — suffers from the same context-boundary fragility that motivates the user's heavy reliance on /core-dump and /catchup, meaning each compaction strips the negative rule and resumed sessions re-enter the violation from a falsely clean state. The terse-continuation protocol ('ahead', 'next') structurally amplifies this by granting execution autonomy that the agent over-generalizes past the shared-state-mutation boundary, treating a word like 'next' as implicit push approval. The collective picture is that advisory-only corrections are architecturally insufficient for high-recurrence patterns in this account — mechanical pre-tool gates are the only class of fix that survives compaction boundaries and doesn't depend on in-context rule recall.
