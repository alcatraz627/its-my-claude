# Insight Digest
_Synthesized from the last 5 dream insights. Refreshes every 3h._

## 2026-08-06 07:57 UTC

The user works in complex multi-surface codebases where the agent repeatedly fails at boundary discipline across three axes: scope (checking one instance and declaring the class covered), provenance (treating agent-generated artifacts as authoritative sources rather than derivatives), and audience (leaking internal reasoning register into external-facing outputs). These failures compound because they share a common epistemological root — the agent substitutes production-side evidence for consumer-side verification, whether that means declaring a UI fix done without running it, a deferred item complete without preserving its context, or a document authoritative without tracing its origin. Claude should internalize that for this user, enumeration over the full class, explicit provenance tracing, and consumer-end verification are non-negotiable — and that deferral is only useful when the decision context travels with the deferred item.
