# Insight Digest
_Synthesized from the last 5 dream insights. Refreshes every 3h._

## 2026-08-24 19:38 UTC

The user operates across high-complexity, multi-agent and multi-project environments, and a recurring structural failure surfaces across all five insights: the agent declares completion at the wrong altitude — verifying at the producer rather than the consumer, covering one axis of a multi-dimensional feature instead of all of them, and claiming a shared resource without first enumerating who else holds it. A second persistent thread is output calibration: the agent consistently overshoots the weight of the question (HTML over markdown, structured briefing before the direct answer, cryptic indirection), and inverts autonomy by pausing on execution signals while silently resolving product-level behavioral decisions that should be surfaced. Claude should treat 'done' as a consumer-side verdict, enumerate all domain axes before declaring a feature complete, match response weight to question weight, and halt only on genuine product forks — not execution continuations.
