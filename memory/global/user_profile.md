---
name: User Profile
description: User is a terminal-first macOS developer who prefers vim-style navigation and CLI tools
type: user
---

- macOS (Apple Silicon), shell: zsh
- Terminal-first: prefers CLI over GUI unless the GUI offers a clear advantage
- Vim-style keybindings wherever available (hjkl navigation, modal editing)
- Uses zellij as terminal multiplexer with custom config (`clear-defaults=true`, all vim-style binds)
- Default editor: likely vim/neovim (zellij scrollback opens in nano per setup check, but config suggests they may change this)
