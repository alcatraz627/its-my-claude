# Skill Runtime Notes

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-08-04 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always require a reception-side proof artifact (a round-trip reply, a rendered screenshot, a file-existence check) before treating any outbound action as successful — sending is never arriving.
- **[conf=0.85]** Always trace any authority claim back to a non-agent-authored source (user spec, running code, actual output) before acting on it — if the only evidence is something this agent or a sibling agent...
- **[conf=0.85]** When fixing or reviewing any instance of a pattern (a component, a list page, a callsite, a visual mode), always enumerate the full class of siblings first — the fix target is the class, and the ...
- **[conf=0.82]** Always distinguish between 'I performed the action' and 'the action had its intended effect' by naming the verification artifact (round-trip reply, runtime output, file-on-disk check) before claimi...
- **[conf=0.82]** Always write deferred items as self-contained decision packets (what, options, tradeoffs, recommended default) that a reader with zero session context can act on without asking a follow-up question.
- **[conf=0.82]** Always require a positive existence proof (round-trip reply, running output, file-on-disk check, non-default value) before reporting success — never infer success from the absence of a failure si...
- **[conf=0.82]** Always consult the canonical external source (design mocks for UI, source code for gap assessments, user-authored specs for authority) before generating any claim that could be derived from it — ...
- **[conf=0.82]** Always name the specific ground-truth artifact (received reply, file on disk, test output, source file content) that would prove the claim, and verify THAT artifact — when the only evidence is on...
- **[conf=0.82]** Always ask 'is this the thing itself or a signal about the thing?' before treating any indirect evidence as confirmation — if it's a proxy, name what direct evidence would look like and obtain it.
- **[conf=0.82]** Always verify at the reception point (rendered page, round-trip reply, file on disk) rather than at the action point (code written, message sent, agent dispatched) before claiming completion.

---

## core-dump: boot-app resource audit [boot-apps-a2] — 2026-08-06 05:05
**Purpose:** Post-reboot audit of autostart apps by resource cost, then log retention, ollama pre-warm removal, and calendar companions.

**Insights:**
1. Three near-misses this session all had one shape: absence of evidence read as evidence of absence, caused by the instrument. A case-sensitive grep said mongo had 0 connections (real: 564,194). A 0-byte nginx access log meant `access_log` was commented out, not that nginx is idle. A retention test "passed" while unable to fail, because nothing exceeded the size threshold. Before believing a zero, ask what else produces that same zero.
2. `/Library/Logs/DiagnosticReports/` is the fastest first read for "the machine feels slow" — macOS writes a timestamped .diag per unresponsive process.
3. `schedule.sh register <plist>` adopts a hand-rolled LaunchAgent into the gcc registry. This matters most for the DELETE side: an event created outside the registry has no recorded calendar_uid, so retiring the cron orphans it. That is how warm-morning survived unnoticed.
4. Truncating a log a process holds open leaves it writing at its old offset and creates a sparse file that still reports the original size. mongod takes SIGUSR1 and reopens; only plain appenders are safe to copy-truncate.
5. MCP servers dwarf everything else: ~2.5 GB across two sessions vs 60 MB for the redis+postgres they wrap, and 501 MB for all boot apps combined. `npm exec <pkg>` costs ~100 MB purely as a wrapper around the node process doing the work.
6. Intelligent disobedience paid off on redis: measured 11 MB and 15 idle wakeups/min on a 64 GB machine, and recommended NOT building the auto-off machinery rather than building what was hinted at.
7. The calendar-companion rule covers every-N-days StartInterval but not sub-daily. A 5-min recurring event would destroy the observability the rule exists for, so a daily marker stating true cadence serves the intent. Deviation recorded in migration 0043 rather than applied silently or skipped.

---

## core-dump: sluggish-UI diagnosis + pm2 idle reaper [slug-ui-7c] — 2026-08-06 01:15
**Purpose:** Diagnose perceived system sluggishness, then build a 12h idle auto-shutoff for pm2-managed services.

**Insights:**
1. `/Library/Logs/DiagnosticReports/` is the highest-signal first read for "the machine feels slow". macOS writes a timestamped .diag whenever a process stops responding; it named Steam in one step after six other avenues had cleared.
2. `ps` %CPU is a lifetime average, so use `top -l` for anything long-lived. WindowServer read 34.6% in ps and 0.0% in top at the same moment after 30 days uptime.
3. Two points are not a trend. A mach-port count that looked like a +1/sec leak went down over a 10-second window.
4. pm2's tracked pid is not the listening pid for npm-wrapped apps. Walk the descendant tree, or every npm-wrapped service reads as portless forever (speedway-fe pid 1154 holds nothing; child node 1248 holds :5101).
5. Connection presence is not use. A Vite HMR websocket from a forgotten browser tab reads as traffic indefinitely, so measure endpoint identity (`endpoints - prior_endpoints`), not count.
6. Exercising caught a guard that inspection would have passed. `svc up`'s port check iterated an empty list on fresh state, so it was silently blind; it needed a pm2-args fallback plus a crash-loop backstop.
7. Two self-inflicted hangs worth remembering: `pmset -g thermlog` streams forever, and `VAR=$(fn)` blocks when fn backgrounds a child inheriting stdout.
8. `prepend-runtime-note.sh` takes `<skill-name> <entry-file>` as arguments and ignores stdin. Piping to it silently no-ops, and an `echo` after it reports a false success.

---

## session: i-dream weekly review + P5 follow-up i-dream-3f970b78 — 2026-08-06

Purpose: run the 2026-08-02 staged weekly audit end to end, then ship the idea behind the one proposal rejected on scope.

- The staged audit at `~/.claude/i-dream/audits/2026-08-02.md` was genuinely pending: 8 surfaced, 5 applied, 3 rejected, fully closed out (outcome line, header, flag).
- A proposal's target path is a hypothesis, not a fact. P5 named an i-dream pipeline change against a `~/.claude/rules/` path; the work actually lived in `~/.claude/scripts/dream/dream-insights.sh` and was mostly already built. Grep for the generator before accepting an audit's framing.
- The precheck-in-TLDR gap sat at `dream-insights.sh:416` — the precheck surfaced only when the description was degenerate, so a well-described high-recurrence pattern hid its actionable gate. S3 at 5x or more now appends it alongside the description.
- `declared-ready-stop.sh` Gate 0 omitted css/scss, which also made the css entry in its own dark-and-light reminder unreachable dead code. Fixed; style-only turns get a soft screenshot note rather than a block, since that file class has no fire-rate history.
- Harness gotcha: that hook needs BOTH a success word and a subject word to register a claim. "Done, padding fixed" registers nothing, so a harness written that way silently tests nothing.
- The prose/copy gates scan whole edit payloads and verbatim data strings; filed `prop-20260805-193505-64`.

---

---

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-30 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always require a reception-side proof artifact (a round-trip reply, a rendered screenshot, a file-existence check) before treating any outbound action as successful — sending is never arriving.
- **[conf=0.85]** Always trace any authority claim back to a non-agent-authored source (user spec, running code, actual output) before acting on it — if the only evidence is something this agent or a sibling agent...
- **[conf=0.85]** When fixing or reviewing any instance of a pattern (a component, a list page, a callsite, a visual mode), always enumerate the full class of siblings first — the fix target is the class, and the ...
- **[conf=0.82]** Always distinguish between 'I performed the action' and 'the action had its intended effect' by naming the verification artifact (round-trip reply, runtime output, file-on-disk check) before claimi...
- **[conf=0.82]** Always write deferred items as self-contained decision packets (what, options, tradeoffs, recommended default) that a reader with zero session context can act on without asking a follow-up question.
- **[conf=0.82]** Always require a positive existence proof (round-trip reply, running output, file-on-disk check, non-default value) before reporting success — never infer success from the absence of a failure si...
- **[conf=0.82]** Always consult the canonical external source (design mocks for UI, source code for gap assessments, user-authored specs for authority) before generating any claim that could be derived from it — ...
- **[conf=0.82]** Always name the specific ground-truth artifact (received reply, file on disk, test output, source file content) that would prove the claim, and verify THAT artifact — when the only evidence is on...
- **[conf=0.82]** Always ask 'is this the thing itself or a signal about the thing?' before treating any indirect evidence as confirmation — if it's a proxy, name what direct evidence would look like and obtain it.
- **[conf=0.82]** Always verify at the reception point (rendered page, round-trip reply, file on disk) rather than at the action point (code written, message sent, agent dispatched) before claiming completion.

---

## core-dump: session studio-jobs-0c, Bengaluru creative-jobs pipeline design — 2026-08-04

**Purpose:** Checkpoint a design session that produced a job-mining guidebook plus two executed modules, and survived three adversarial peer reviews.

**Insights:**

1. The template's H1 lines carry em-dashes and the prose-lint Stop hook blocks them at budget zero, so the first Write of every full core-dump fails. Resolving it needs `~/.claude/skills/catchup/SKILL.md:456`, which confirms only the `##` headings are the parse contract, so the H1 separator is free to change. Filed as prop-20260804-140342-20.
2. A greenfield project has no `.claude/`, so Phase 3.1's "find the directory containing `.claude/`" finds nothing and Phase 3.7's workspace diff has no doc to diff. Both degrade fine; use CWD as project root and skip 3.7 silently.
3. Phase 3.5's `mcp__inputs__form` is unavailable when the Interactive Inputs MCP is not connected. The silent-default fallback worked as specified; no need to substitute AskUserQuestion.
4. Seeding Pending Items from the live Task list caught only 2 of 11 real pending items this session. The conversation carried the rest (an open bug, seven unapplied adoptions, three raised-not-acted rubric changes). The spec's "then add anything the conversation surfaced" is doing most of the work, not the task read.
5. Standing caveats earned their field here. Three separate unverified constants (a guessed 1.45 multiplier, a guessed 75-day threshold, a suspect 7.0km geocode) plus an openly unfixed bug would all have been laundered into confident-sounding doctrine by a summary that only captured decisions.

---

---

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-29 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always require a reception-side proof artifact (a round-trip reply, a rendered screenshot, a file-existence check) before treating any outbound action as successful — sending is never arriving.
- **[conf=0.85]** Always trace any authority claim back to a non-agent-authored source (user spec, running code, actual output) before acting on it — if the only evidence is something this agent or a sibling agent...
- **[conf=0.85]** When fixing or reviewing any instance of a pattern (a component, a list page, a callsite, a visual mode), always enumerate the full class of siblings first — the fix target is the class, and the ...
- **[conf=0.82]** Always distinguish between 'I performed the action' and 'the action had its intended effect' by naming the verification artifact (round-trip reply, runtime output, file-on-disk check) before claimi...
- **[conf=0.82]** Always write deferred items as self-contained decision packets (what, options, tradeoffs, recommended default) that a reader with zero session context can act on without asking a follow-up question.
- **[conf=0.82]** Always require a positive existence proof (round-trip reply, running output, file-on-disk check, non-default value) before reporting success — never infer success from the absence of a failure si...
- **[conf=0.82]** Always consult the canonical external source (design mocks for UI, source code for gap assessments, user-authored specs for authority) before generating any claim that could be derived from it — ...
- **[conf=0.82]** Always name the specific ground-truth artifact (received reply, file on disk, test output, source file content) that would prove the claim, and verify THAT artifact — when the only evidence is on...
- **[conf=0.82]** Always ask 'is this the thing itself or a signal about the thing?' before treating any indirect evidence as confirmation — if it's a proxy, name what direct evidence would look like and obtain it.
- **[conf=0.82]** Always verify at the reception point (rendered page, round-trip reply, file on disk) rather than at the action point (code written, message sent, agent dispatched) before claiming completion.

---

## session: manual-review round verified catch-state-7f · 2026-07-30
Purpose: full core-dump after PR #8 finalize, docs PR #9, and the manual-review verification round.
- Items 1/2/3/9 of the owner list were already fixed by earlier rounds; proved live instead of rebuilding.
- The one build was the Error label map (REASON_LABELS/reasonKeyOf), commit eb6cbdf.
- Dump at _20260730-catch-state-7f.claude.md, indexed as manualreview-round-verified.
- Skipped prettier, name prompt, workspace diff, visual summary to hold context.

---

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-29 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always require a reception-side proof artifact (a round-trip reply, a rendered screenshot, a file-existence check) before treating any outbound action as successful — sending is never arriving.
- **[conf=0.85]** Always trace any authority claim back to a non-agent-authored source (user spec, running code, actual output) before acting on it — if the only evidence is something this agent or a sibling agent...
- **[conf=0.85]** When fixing or reviewing any instance of a pattern (a component, a list page, a callsite, a visual mode), always enumerate the full class of siblings first — the fix target is the class, and the ...
- **[conf=0.82]** Always distinguish between 'I performed the action' and 'the action had its intended effect' by naming the verification artifact (round-trip reply, runtime output, file-on-disk check) before claimi...
- **[conf=0.82]** Always write deferred items as self-contained decision packets (what, options, tradeoffs, recommended default) that a reader with zero session context can act on without asking a follow-up question.
- **[conf=0.82]** Always require a positive existence proof (round-trip reply, running output, file-on-disk check, non-default value) before reporting success — never infer success from the absence of a failure si...
- **[conf=0.82]** Always consult the canonical external source (design mocks for UI, source code for gap assessments, user-authored specs for authority) before generating any claim that could be derived from it — ...
- **[conf=0.82]** Always name the specific ground-truth artifact (received reply, file on disk, test output, source file content) that would prove the claim, and verify THAT artifact — when the only evidence is on...
- **[conf=0.82]** Always ask 'is this the thing itself or a signal about the thing?' before treating any indirect evidence as confirmation — if it's a proxy, name what direct evidence would look like and obtain it.
- **[conf=0.82]** Always verify at the reception point (rendered page, round-trip reply, file on disk) rather than at the action point (code written, message sent, agent dispatched) before claiming completion.

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-29 — 9 insights (conf>=0.80) -->

- **[conf=0.88]** Always require a reception-side proof artifact (a round-trip reply, a rendered screenshot, a file-existence check) before treating any outbound action as successful — sending is never arriving.
- **[conf=0.85]** Always trace any authority claim back to a non-agent-authored source (user spec, running code, actual output) before acting on it — if the only evidence is something this agent or a sibling agent...
- **[conf=0.85]** When fixing or reviewing any instance of a pattern (a component, a list page, a callsite, a visual mode), always enumerate the full class of siblings first — the fix target is the class, and the ...
- **[conf=0.82]** Always distinguish between 'I performed the action' and 'the action had its intended effect' by naming the verification artifact (round-trip reply, runtime output, file-on-disk check) before claimi...
- **[conf=0.82]** Always write deferred items as self-contained decision packets (what, options, tradeoffs, recommended default) that a reader with zero session context can act on without asking a follow-up question.
- **[conf=0.82]** Always require a positive existence proof (round-trip reply, running output, file-on-disk check, non-default value) before reporting success — never infer success from the absence of a failure si...
- **[conf=0.82]** Always consult the canonical external source (design mocks for UI, source code for gap assessments, user-authored specs for authority) before generating any claim that could be derived from it — ...
- **[conf=0.82]** Always name the specific ground-truth artifact (received reply, file on disk, test output, source file content) that would prove the claim, and verify THAT artifact — when the only evidence is on...
- **[conf=0.82]** Always ask 'is this the thing itself or a signal about the thing?' before treating any indirect evidence as confirmation — if it's a proxy, name what direct evidence would look like and obtain it.

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-28 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.88]** Always require a reception-side proof artifact (a round-trip reply, a rendered screenshot, a file-existence check) before treating any outbound action as successful — sending is never arriving.
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.85]** Always trace any authority claim back to a non-agent-authored source (user spec, running code, actual output) before acting on it — if the only evidence is something this agent or a sibling agent...
- **[conf=0.85]** When fixing or reviewing any instance of a pattern (a component, a list page, a callsite, a visual mode), always enumerate the full class of siblings first — the fix target is the class, and the ...
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** Always include the prior decision context, concrete options, and the reason for stopping in any user-facing question or halt — if the user cannot act without a follow-up question, the message is ...
- **[conf=0.82]** Always distinguish between 'I performed the action' and 'the action had its intended effect' by naming the verification artifact (round-trip reply, runtime output, file-on-disk check) before claimi...
- **[conf=0.82]** Always write deferred items as self-contained decision packets (what, options, tradeoffs, recommended default) that a reader with zero session context can act on without asking a follow-up question.
- **[conf=0.82]** Always require a positive existence proof (round-trip reply, running output, file-on-disk check, non-default value) before reporting success — never infer success from the absence of a failure si...

---

## catchup: resume from catch-task-9f dump · 2026-07-29 00:45

**Purpose:** restore context after session-end compaction; rehydrate task list; state briefing.

**Insights:**
1. Reality drift caught by the git cross-check: the dump's top blocker (24+8
   unpushed commits awaiting the owner's push command) had already resolved.
   `git rev-list --count origin/main..main` is 0 in BOTH repos. Verify unpushed
   counts before re-asking the owner for push approval.
2. The owner reorganized `user_docs/speedway-todo.md` (uncommitted, owner lane):
   several items flipped [x] to [ ] (export one-click, workflow halts, setup-first
   create sub-items, PCdb suggestion), the hands-on-test lines were deleted, and
   one new deploy item appeared (point app at kit 0.0.10, redeploy staging).
   Treat the flips as fresh owner signal, not regressions proven in code.
3. Task list rehydrated with 22 items: 21 from the f1378236 sync block plus the
   em-dash caveat pair, original #n kept in subjects.

---

## core-dump: full checkpoint for the kanban mega-session [gcc-kanban] (2026-07-29)

**Purpose:** Snapshot the v3→v7 kanban session (build, three review lanes, design handoff) so the next session continues from HANDOFF.md, and refresh the gcc's durable channels about the built system.

**Insights:**

1. The "make gcc aware" half of this dump was mostly a MEMORY update: the project memory still said "not yet built" four days after the build shipped. Stale project memories mislead every future session; refreshing them belongs in the dump ritual whenever a subsystem crossed plan→built this session.
2. The prose gate (zero connective em-dashes) now fires on memory files, MEMORY.md index lines, and even this note's own heading template; the index's "dash + hook" convention no longer passes. New lines use a colon or parentheses.
3. Resume Contract's Standing caveats carried four atone ids and two contested review findings verbatim; that is the layer that keeps the next agent from re-applying the designer findings the owner rerouted to the handoff.
4. The inputs form (Phase 3.5) was skipped by standing memory (dialogs unusable in the owner's fullscreen TUI); defaults plus explicit name text worked fine.

---

---

## designer-reviewer: reviewed kanban-v7 terminal skin (dark + light) — 2026-07-28 00:15

**Purpose:** Scored critique of the kanban board after its terminal-skin rebuild + two fix rounds, both themes, judged against the fingerprints AND the STORIES.md experience frame (glance/triage/note-drop/provenance/label-lens/stale-audit).

**Insights:**

1. When a review has an experience-frame doc (STORIES.md), read it before scoring — half the real findings (amber dilution vs "one attention color", raw tag grammar in the skim layer) come from the frame's own stated requirements, not the generic fingerprints.
2. `see --ocr` caught two findings native vision alone would have under-confidenced: a literal duplicated chip (`/skeptical-review` twice) and a garbled left-truncated ref (`_ession-notes/…`). The ~300ms OCR pass is worth running on any UI with dense chip/ref text.
3. Tag-grammar UIs (notes carrying `!now /skill >lane #tag`) have a systematic double-render trap: the raw note text AND the parsed chips both display, tripling the same tokens. Check any card that renders both a free-text line and derived chips.
4. Semantic-color audits should count WHERE each hue appears per column, not just what it maps to — amber mapped "correctly" to attention here but appeared in 4 of 5 columns, which destroys the blocked-column signal the stories demanded.

---

---

---

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-28 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.88]** Always require a reception-side proof artifact (a round-trip reply, a rendered screenshot, a file-existence check) before treating any outbound action as successful — sending is never arriving.
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.85]** Always trace any authority claim back to a non-agent-authored source (user spec, running code, actual output) before acting on it — if the only evidence is something this agent or a sibling agent...
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** Always represent missing/unknown inputs as null or an explicit uncertainty marker, never as a zero, false, or permissive default — when a branch handles 'no data', its output must be distinguisha...
- **[conf=0.82]** Always include the prior decision context, concrete options, and the reason for stopping in any user-facing question or halt — if the user cannot act without a follow-up question, the message is ...
- **[conf=0.82]** Always distinguish between 'I performed the action' and 'the action had its intended effect' by naming the verification artifact (round-trip reply, runtime output, file-on-disk check) before claimi...

---


---

## session: content-gen sprint close [speed-catch-7e] — 2026-07-28

Purpose: build Content Generation v1 against a written charter, fix the job/
parts UI the owner reviewed, then close the sprint with docs + commits.

**Insights:**
1. The gate IS the skill. /bloop run without Phase 4 shipped clean-looking
   work; run WITH it, it found two real defects self-review missed (a fixture
   blind by construction, a facet whose options were not a partition) and
   confirmed a regression its question had prompted. Recorded S3 atone
   mist-20260727-223728-20; juror agreed the skip was very-wrong.
2. Verify a delegated agent's artifact EXISTS before trusting completion. Two
   of four review agents went idle without writing their report; one produced
   nothing at all. The 23 adversarial findings exist only because the file was
   checked and the agent sent back.
3. rg output was mangled all session (matched text replaced with "n"), which
   produced a confident "nothing renders content" claim that was false. Open
   the file; never conclude absence from a derived signal.
4. Structural UI complaints are container-contract questions. "The page doesn't
   scroll" was one line in the route handle (layout:"list" vs "flow"), after
   hours of styling around it.
5. A Promise.race timeout does not free a hung socket — only AbortSignal does.
   Stranded requests poisoned the pool and starved every later call.

---

---

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-28 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.88]** Always require a reception-side proof artifact (a round-trip reply, a rendered screenshot, a file-existence check) before treating any outbound action as successful — sending is never arriving.
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.85]** Always trace any authority claim back to a non-agent-authored source (user spec, running code, actual output) before acting on it — if the only evidence is something this agent or a sibling agent...
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** Always represent missing/unknown inputs as null or an explicit uncertainty marker, never as a zero, false, or permissive default — when a branch handles 'no data', its output must be distinguisha...
- **[conf=0.82]** Always include the prior decision context, concrete options, and the reason for stopping in any user-facing question or halt — if the user cannot act without a follow-up question, the message is ...
- **[conf=0.82]** Always distinguish between 'I performed the action' and 'the action had its intended effect' by naming the verification artifact (round-trip reply, runtime output, file-on-disk check) before claimi...

---

## session: language-quality sweep + bake [simple-lang] — 2026-07-28

Purpose: turn "reduce slop/ambiguous/misleading language" into a measured, user-ratified taxonomy + tooling (STE kit evaluated as one candidate, not adopted).

Insights:
- Single-label classification of multi-defect prose plateaus at kappa 0.3-0.6 with label-collapse whack-a-mole (each spec iteration moves which label becomes the catch-all). Recall-biased multi-label flagging + mechanical post-guards + downstream adversarial confirm is the working shape.
- The sonnet-beats-local assumption inverted here: gemma4:26b (local, $0) out-graded sonnet on this judgment task; canary before assuming lane quality.
- `q` blocks forever on an open piped stdin — every "server wedge" was this one missing stdin=DEVNULL. Same family as the read-</dev/null gotcha.
- Consumed-but-unpersisted approvals break the evidence chain (decision-page answer --consume + context-only rulings read as "fabricated" to a fresh reviewer). Persist on consume, immediately. Pinned: pin-20260727214714-23.
- The user's verdict-first category (facts before conclusions; user owns "done") came from their decision-page NOTES, not from any model lane — the free-text escape hatch out-discovered the novel-label mechanism.
- prose-lint self-scoring on style docs is inherent (mention vs use); strip blockquotes + table rows, then document the residue rather than special-casing files.
---

---

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-27 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.88]** Always require a reception-side proof artifact (a round-trip reply, a rendered screenshot, a file-existence check) before treating any outbound action as successful — sending is never arriving.
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.85]** Always trace any authority claim back to a non-agent-authored source (user spec, running code, actual output) before acting on it — if the only evidence is something this agent or a sibling agent...
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** Always represent missing/unknown inputs as null or an explicit uncertainty marker, never as a zero, false, or permissive default — when a branch handles 'no data', its output must be distinguisha...
- **[conf=0.82]** Always include the prior decision context, concrete options, and the reason for stopping in any user-facing question or halt — if the user cannot act without a follow-up question, the message is ...
- **[conf=0.82]** Always distinguish between 'I performed the action' and 'the action had its intended effect' by naming the verification artifact (round-trip reply, runtime output, file-on-disk check) before claimi...

---

## catchup: resume from adrev-kanbn-4b checkpoint [gcc-kanban] — 2026-07-27 14:45

**Purpose:** Restore the kanban + adversarial-review session context into a fresh session from the explicit checkpoint file.

**Insights:**

1. Explicit-filename arg path is clean: full Resume Contract present, no picker/resolve needed; the whole resume took ~10 tool calls.
2. `decision-page.sh status` awaiting-list is the fastest check for whether a blocked-on decision page got answered (kanban-ui-0727 still awaiting; no answer file in its dir).
3. The predecessor alias `adrev-kanbn-4b` still heartbeats as live (sinceSeenS 256 < its 5-min watcher re-register loop). /catchup onto a still-living session's checkpoint creates a watcher-ownership question: its Monitor on the answer file may still be armed, so re-arming here would double-wake. Check `claude-ipc peers` sinceSeenS before arming; surface the choice to the user instead of auto-arming.
4. Glob tool was unavailable in this session; ls/find via Bash covered it.

---

---

## session: adversarial-review skill + kanban board system + P0-P3 fix plan [adrev-kanbn-4b] — 2026-07-27

**Purpose:** Build /adversarial-review + a throwaway agent-driven kanban board; adversarially review, UX-test, and fix-plan the board through /bloop.

**Insights:**

- For a product NAMED after a known artifact class (kanban), the class anatomy (columns/labels/sub-items) IS the spec — a distinctiveness-driven redesign that exits the class is the failure, not a feature. Two misfires this session (v2 ledger, missing labels/subs) both traced to optimizing the frontend-design brief over the named class. Atone `rebuild-replaced-accumulated-ux-without-parity-audit` now 3×.
- Decision-page submit-to-wake (Monitor on the answer file, armed in the SAME turn as handoff) works reliably across 22h/43h idle gaps — user-affirmed. The failure mode is ending the turn without arming; the fix is a same-turn discipline, now baked into pending-add output.
- Parallel /skeptical-review + /bloop validation gate converging independently on the same destructive blocker (unregister wrong-target) is the strongest real-finding signal. Worth the double spend on genuinely-destructive surfaces.
- A mutation test that stays GREEN means the fix structure is the bug: a redundant pre-lock check masked the throw-based lock release and had to be removed to make the guard pinnable.
- Behavior-anchored fix plans (observed→executable-target→change→probe added to a standing action script) make "done" mechanical and give every future change a regression gate.

---

---

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-27 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.88]** Always require a reception-side proof artifact (a round-trip reply, a rendered screenshot, a file-existence check) before treating any outbound action as successful — sending is never arriving.
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.85]** Always trace any authority claim back to a non-agent-authored source (user spec, running code, actual output) before acting on it — if the only evidence is something this agent or a sibling agent...
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** When fixing an instance of a problem, always ask 'what class does this instance belong to?' and enumerate the class before scoping the fix — the user's named examples are a sample, not an exhaust...
- **[conf=0.82]** Always represent missing/unknown inputs as null or an explicit uncertainty marker, never as a zero, false, or permissive default — when a branch handles 'no data', its output must be distinguisha...
- **[conf=0.82]** Always include the prior decision context, concrete options, and the reason for stopping in any user-facing question or halt — if the user cannot act without a follow-up question, the message is ...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-25 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** When fixing an instance of a problem, always ask 'what class does this instance belong to?' and enumerate the class before scoping the fix — the user's named examples are a sample, not an exhaust...
- **[conf=0.82]** Always represent missing/unknown inputs as null or an explicit uncertainty marker, never as a zero, false, or permissive default — when a branch handles 'no data', its output must be distinguisha...
- **[conf=0.82]** Always include the prior decision context, concrete options, and the reason for stopping in any user-facing question or halt — if the user cannot act without a follow-up question, the message is ...
- **[conf=0.82]** Always distinguish between 'I performed the action' and 'the action had its intended effect' by naming the verification artifact (round-trip reply, runtime output, file-on-disk check) before claimi...
- **[conf=0.80]** Always verify the end-state that matters (message received, code path executed, behavior observed) rather than a proxy signal (send log, type-check, test collection) — if the verification doesn't...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-25 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** When fixing an instance of a problem, always ask 'what class does this instance belong to?' and enumerate the class before scoping the fix — the user's named examples are a sample, not an exhaust...
- **[conf=0.82]** Always represent missing/unknown inputs as null or an explicit uncertainty marker, never as a zero, false, or permissive default — when a branch handles 'no data', its output must be distinguisha...
- **[conf=0.82]** Always include the prior decision context, concrete options, and the reason for stopping in any user-facing question or halt — if the user cannot act without a follow-up question, the message is ...
- **[conf=0.82]** Always distinguish between 'I performed the action' and 'the action had its intended effect' by naming the verification artifact (round-trip reply, runtime output, file-on-disk check) before claimi...
- **[conf=0.80]** Always verify the end-state that matters (message received, code path executed, behavior observed) rather than a proxy signal (send log, type-check, test collection) — if the verification doesn't...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-25 — 9 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** When fixing an instance of a problem, always ask 'what class does this instance belong to?' and enumerate the class before scoping the fix — the user's named examples are a sample, not an exhaust...
- **[conf=0.82]** Always represent missing/unknown inputs as null or an explicit uncertainty marker, never as a zero, false, or permissive default — when a branch handles 'no data', its output must be distinguisha...
- **[conf=0.82]** Always include the prior decision context, concrete options, and the reason for stopping in any user-facing question or halt — if the user cannot act without a follow-up question, the message is ...
- **[conf=0.80]** Always verify the end-state that matters (message received, code path executed, behavior observed) rather than a proxy signal (send log, type-check, test collection) — if the verification doesn't...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-23 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** Always identify what layer of indirection separates the available evidence from the ground truth; when the gap is >0 layers (log vs delivery, compile vs execute, default vs measured, cache vs fresh...
- **[conf=0.82]** When fixing an instance of a problem, always ask 'what class does this instance belong to?' and enumerate the class before scoping the fix — the user's named examples are a sample, not an exhaust...
- **[conf=0.82]** Always represent missing/unknown inputs as null or an explicit uncertainty marker, never as a zero, false, or permissive default — when a branch handles 'no data', its output must be distinguisha...
- **[conf=0.82]** Always include the prior decision context, concrete options, and the reason for stopping in any user-facing question or halt — if the user cannot act without a follow-up question, the message is ...

---

## release-changelog: v5.2.5 hotfix notes from PR #269 — 2026-07-24 15:30

**Purpose:** Turn a 2-file / +36−8 follow-up release PR into a changelog, without the per-theme fan-out the skill prescribes for large diffs.

**Insights:**

1. NEW commit-subject trap: **a subject naming one surface can hide a second.** Both PRs here were titled for preview surfaces (`fix(enhancement) ... [render preview]`, `fix(preview-data-history)`) and the frontend code comment said "Display-only", but the backend change landed in `remove_ctx_fields`, which serves preview AND export, applied before the `is_export` branch. Exports changed too. Rule of thumb: when a fix touches a shared formatter, trace which modes route through it before repeating the subject's scope claim.
2. The version-bump check earns its place a third time. `origin/release` and `origin/development` both read `5.2.4`, and 5.2.4 had already shipped as PR #264, so the PR title "Release V5.2.4" was a carried-over title. Always diff the version against the *previously shipped* notes folder, not just base-vs-head.
3. Under ~5 changed files, skip the fan-out entirely and read the diff inline. Total cost here was ~10 tool calls; a per-theme dispatch would have cost more than the diff itself.
4. Check whether a `release-v-<ver>/` folder already exists before choosing the output path. `release-v-5.2.4/` held PR #264's notes; writing there would have clobbered a 27 KB artifact. Reading its blockquote header (which PR + file count it covered) is the cheapest way to tell a re-cut from a follow-up.
5. For a small diff, verifying the *mechanism* is where the time goes, not reading the hunks: `get_column_map_item`'s fail-closed behaviour, whether `restore_brand_code` mutates or copies, and which transform chain includes `get_item_result` were the three reads that made the caveats section factual rather than speculative.

---

---

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-23 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** Always identify what layer of indirection separates the available evidence from the ground truth; when the gap is >0 layers (log vs delivery, compile vs execute, default vs measured, cache vs fresh...
- **[conf=0.82]** When fixing an instance of a problem, always ask 'what class does this instance belong to?' and enumerate the class before scoping the fix — the user's named examples are a sample, not an exhaust...
- **[conf=0.82]** Always represent missing/unknown inputs as null or an explicit uncertainty marker, never as a zero, false, or permissive default — when a branch handles 'no data', its output must be distinguisha...
- **[conf=0.82]** Always include the prior decision context, concrete options, and the reason for stopping in any user-facing question or halt — if the user cannot act without a follow-up question, the message is ...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-23 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** Always verify from the RECEIVING end (the peer's reply, the runtime output, the cache-busted result) rather than from the SENDING end when claiming an effect occurred.
- **[conf=0.82]** Always identify what layer of indirection separates the available evidence from the ground truth; when the gap is >0 layers (log vs delivery, compile vs execute, default vs measured, cache vs fresh...
- **[conf=0.82]** When fixing an instance of a problem, always ask 'what class does this instance belong to?' and enumerate the class before scoping the fix — the user's named examples are a sample, not an exhaust...
- **[conf=0.82]** Always represent missing/unknown inputs as null or an explicit uncertainty marker, never as a zero, false, or permissive default — when a branch handles 'no data', its output must be distinguisha...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-23 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** When a user reports an issue on one instance, always grep for the full class of sibling instances sharing the same pattern before scoping the fix — the user's named example is a sample, not an ex...
- **[conf=0.82]** Always verify from the RECEIVING end (the peer's reply, the runtime output, the cache-busted result) rather than from the SENDING end when claiming an effect occurred.
- **[conf=0.82]** Always identify what layer of indirection separates the available evidence from the ground truth; when the gap is >0 layers (log vs delivery, compile vs execute, default vs measured, cache vs fresh...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-23 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...
- **[conf=0.82]** When a user reports an issue on one instance, always grep for the full class of sibling instances sharing the same pattern before scoping the fix — the user's named example is a sample, not an ex...
- **[conf=0.82]** Always verify from the RECEIVING end (the peer's reply, the runtime output, the cache-busted result) rather than from the SENDING end when claiming an effect occurred.

---

## core-dump: full dump, three-arc session (cl-instances) — 2026-07-23 09:35

**Purpose:** Checkpoint a 3-arc session (meld PH2 dark-ship, vision retest, hub perf 13.6s→31ms) with "and push" as accompanying directive.

**Insights:**

1. "and push" args parse as instructions, not filename — the right handling was running the push attempt FIRST so the gate's sentinel ask reaches the user while the dump writes; the sentinel is single-use and in-chat approval never substitutes.
2. Standing constraints now span TWO arcs (meld doctrine + the owner's prime-before-paint comment); the perf arc ADDED a constraint mid-session — the carry-forward list grows, it never rotates out without owner retirement.
3. Skipped the 3.5 name form under a terse two-word invocation ("never block here" clause) with a descriptive default name; noting the skip in output keeps it legible.

---

---

## improve-skill: pr-review output contract rewritten — 2026-07-23 02:10

**Purpose:** Replace pr-review's machine wire-format return with a delivered-file + human-digest contract, matching /release-changelog, and fix four defects a live run on PR #264 exposed.

**Insights:**

1. A rule addressed to a role that never reads the file cannot be delivered. pr-review's "the parent must not paste this block" was added 2026-07-21 to stop a wire-format leak; on the next run the block AND that sentence were both printed to the user. A forked skill's only channel to its caller is its return text, so an instruction about the return can only travel inside the return. When a fix targets a reader who does not exist, delete the artifact instead of instructing around it.
2. `gh pr diff <N>` returns HTTP 406 above 300 files — precisely the release-PR case. Any skill documenting it as the diff source needs the `git diff origin/<base>...origin/<head>` three-dot fallback, or it fails on the PRs that matter most.
3. Bare-basename citations are a correctness bug, not an ergonomics one. `backend/lib/agents/EnhancementAgentV3.py` and `backend/lib/pipeline/methods/enhancement_agent_v3.py` are different files whose basenames collide on a case-insensitive filesystem. The mechanical check is `rg -o '`[^`/]+\.(ts|py):[0-9]'` over the finished report.
4. A skill should not promise output shape a repo hook controls. pr-review forbade "fixed-width column padding" while a project PostToolUse hook ran `prettier --write` on every markdown write. Softened to a preference rather than left as an unkeepable rule.
5. pr-review had no post-run-insights phase and correspondingly zero runtime-notes entries after multiple runs. Absence of runtime history is diagnostic: check whether the skill has a step that writes it before concluding it has never run.
6. Scope note from the user: pr-review should stay single-pass. Fan-out belongs in /release-changelog, where per-theme readers each own distinct files; a PR review applies the same conventions to every file, so sharding costs more than it buys.

---

---

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-22 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-21 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.88]** Always update the Task tool after every logical unit of work regardless of edit velocity — treat task updates as a mandatory post-edit step, not a low-priority bookkeeping chore that can be batched.
- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.85]** Always grep for the full class of affected sites before scoping a fix when the user names a specific instance — the named instance is a sample, not the boundary.
- **[conf=0.83]** Always surface the exact unblock command and halt when hitting an external hard block (auth, credential, harness guard) — never attempt workarounds or silent retries for blocks outside your scope.
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-21 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** Always enumerate all instances of the affected class (grep the full tree for the component/pattern/policy) before writing any fix code, and include the instance count in the commit message so the s...
- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.85]** Before claiming verified/done/works, ask: 'Is what I just observed the step the claim is actually about, or a proxy one hop upstream?' — if it's a proxy (send not receive, compile not run, cached...
- **[conf=0.82]** Always distinguish proxy evidence from direct evidence before claiming verification — if the check did not observe the actual outcome (message received, code path executed, behavior triggered, co...
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...
- **[conf=0.82]** When a user reports an issue on one instance, always grep for the full class of sibling instances sharing the same pattern before scoping the fix — the user's named example is a sample, not an ex...
- **[conf=0.82]** Always verify from the RECEIVING end (the peer's reply, the runtime output, the cache-busted result) rather than from the SENDING end when claiming an effect occurred.

---

## create-skill: adversarial-review + kanban design definition [adrev-kanbn-4b] — 2026-07-21 19:50

**Purpose:** Build the /adversarial-review skill (prosecution-style sibling of /skeptical-review) and write the kanban-board design definition without building it.

**Insights:**

1. The user pre-answered create-skill's Q&A in their prompt; formalizing from the spec and presenting the plan in the summary beats blocking on seven sequential confirms when the user is away.
2. Differentiating a new review lane from /skeptical-review hinged on two structural deltas: the adversarial lane EXECUTES (worktree-isolated) instead of only reading, and it carries a relevance gate mined from the user's recorded values (guidance, style/derived, atone lineage) so harshness stays undismissable.
3. The persona file carries the full role contract and the SKILL.md dispatch pastes it verbatim — avoids the drift risk of duplicating the prompt in both places (skeptical-review duplicates; consider migrating it too).
4. decision-page.sh accepts a decisions-only config (sections: []) and check passes; groups declared but unassigned to decisions are harmless.
5. For design-only asks ("don't one-shot this"), the deliverable set that works: DESIGN.md in assets/reports + decision page with recommendations + a project memory pointing at both, so any future session can resume from the verdicts.

---

---

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-21 — 10 insights (conf>=0.80) -->

- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.82]** Always distinguish proxy evidence from direct evidence before claiming verification — if the check did not observe the actual outcome (message received, code path executed, behavior triggered, co...
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...
- **[conf=0.82]** When a user reports an issue on one instance, always grep for the full class of sibling instances sharing the same pattern before scoping the fix — the user's named example is a sample, not an ex...
- **[conf=0.82]** Always verify from the RECEIVING end (the peer's reply, the runtime output, the cache-busted result) rather than from the SENDING end when claiming an effect occurred.
- **[conf=0.82]** Always identify what layer of indirection separates the available evidence from the ground truth; when the gap is >0 layers (log vs delivery, compile vs execute, default vs measured, cache vs fresh...
- **[conf=0.82]** When fixing an instance of a problem, always ask 'what class does this instance belong to?' and enumerate the class before scoping the fix — the user's named examples are a sample, not an exhaust...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-20 — 10 insights (conf>=0.80) -->

- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.82]** Always distinguish proxy evidence from direct evidence before claiming verification — if the check did not observe the actual outcome (message received, code path executed, behavior triggered, co...
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...
- **[conf=0.82]** When a user reports an issue on one instance, always grep for the full class of sibling instances sharing the same pattern before scoping the fix — the user's named example is a sample, not an ex...
- **[conf=0.82]** Always verify from the RECEIVING end (the peer's reply, the runtime output, the cache-busted result) rather than from the SENDING end when claiming an effect occurred.
- **[conf=0.82]** Always identify what layer of indirection separates the available evidence from the ground truth; when the gap is >0 layers (log vs delivery, compile vs execute, default vs measured, cache vs fresh...
- **[conf=0.82]** When fixing an instance of a problem, always ask 'what class does this instance belong to?' and enumerate the class before scoping the fix — the user's named examples are a sample, not an exhaust...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-20 — 10 insights (conf>=0.80) -->

- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.82]** Always distinguish proxy evidence from direct evidence before claiming verification — if the check did not observe the actual outcome (message received, code path executed, behavior triggered, co...
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...
- **[conf=0.82]** When a user reports an issue on one instance, always grep for the full class of sibling instances sharing the same pattern before scoping the fix — the user's named example is a sample, not an ex...
- **[conf=0.82]** Always verify from the RECEIVING end (the peer's reply, the runtime output, the cache-busted result) rather than from the SENDING end when claiming an effect occurred.
- **[conf=0.82]** Always identify what layer of indirection separates the available evidence from the ground truth; when the gap is >0 layers (log vs delivery, compile vs execute, default vs measured, cache vs fresh...
- **[conf=0.82]** When fixing an instance of a problem, always ask 'what class does this instance belong to?' and enumerate the class before scoping the fix — the user's named examples are a sample, not an exhaust...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-20 — 10 insights (conf>=0.80) -->

- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.82]** When a user signal (reassurance, complaint, permission) arrives, always ask 'what is the SCOPE of this signal?' separately from 'what is the DIRECTION?' — a positive signal scoped to comfort does...
- **[conf=0.82]** Always distinguish proxy evidence from direct evidence before claiming verification — if the check did not observe the actual outcome (message received, code path executed, behavior triggered, co...
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...
- **[conf=0.82]** When a user reports an issue on one instance, always grep for the full class of sibling instances sharing the same pattern before scoping the fix — the user's named example is a sample, not an ex...
- **[conf=0.82]** Always verify from the RECEIVING end (the peer's reply, the runtime output, the cache-busted result) rather than from the SENDING end when claiming an effect occurred.
- **[conf=0.82]** Always identify what layer of indirection separates the available evidence from the ground truth; when the gap is >0 layers (log vs delivery, compile vs execute, default vs measured, cache vs fresh...

---

## designer-reviewer: reviewed claude-ipc i-dashboard TUI frames — 2026-07-20 12:20

**Purpose:** Scored aesthetic critique of the 15-frame `tui-frames/` capture set (feat/i-dashboard branch) — peers/inbox/projects/orphans/log views plus compose, copy-menu, filter, quit-guard, and 80x24 frames.

**Insights:**

1. For a TUI frame SET, score the primary frame but sweep every distinct surface — the highest-value findings (key-value color inversion between detail pane and copy menu, footer-vs-help key styling mismatch) are only visible by comparing frames, never within one.
2. Both `see --ui` and `see --ocr` silently substitute readable glyphs ("•", "z") for tofu boxes — glyph-coverage defects are only detectable in the native raster read. Never cite OCR as evidence about glyph identity.
3. Whole-row status coloring plus no selection bar means selection and liveness compete for the same foreground-color channel — check for a dedicated cursor affordance (inverse video / prefix marker) first in any list-based TUI.
4. The skill's CSS-fix format maps cleanly to TUI form — express fixes as style-token assignments (fg/bg/attr per role) rather than CSS properties.

---

---

## core-dump: meld-ph2-handoff (claude-instances) — 2026-07-20

**Purpose:** Full hand-off dump at ~80% ctx for a successor to run meld PH2+.

**Insights:**

1. A session spanning 4 arcs (audit tail, meld design, magi, phase builds)
   checkpoints best by pointing at its own artifacts (plan v2, magi archive,
   workspace todos) rather than re-narrating them — the Resume Contract
   carried 6 constraints incl. a DEFERRED-GATE debt that must not launder.
2. Passing --name to the pointer writer and skipping the interactive form is
   the right call at high context; the picker label matters more than the
   ceremony.

---

---

## core-dump: dream-catch-e2 full dump — 2026-07-20

**Purpose:** End-of-arc checkpoint after the 8-task + six-item-queue marathon (5 gates, 2 repos, all shipped).

**Insights:**

1. A two-day multi-repo arc dumps cleanly if the workspace ledger was maintained per-milestone — the checkpoint was synthesis, not archaeology.
2. Standing-caveat hygiene: three of the inherited caveats CLOSED with live evidence this arc (cron health, step-back, pins lane); the dump must say "closed" explicitly or /catchup re-carries them forever.
3. The Resume Contract's "expired authorizations" earned its keep twice — both push sentinels were consumed mid-arc and a fresh one was needed each time.

---

---

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-19 — 10 insights (conf>=0.80) -->

- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.82]** When a user signal (reassurance, complaint, permission) arrives, always ask 'what is the SCOPE of this signal?' separately from 'what is the DIRECTION?' — a positive signal scoped to comfort does...
- **[conf=0.82]** Always distinguish proxy evidence from direct evidence before claiming verification — if the check did not observe the actual outcome (message received, code path executed, behavior triggered, co...
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...
- **[conf=0.82]** When a user reports an issue on one instance, always grep for the full class of sibling instances sharing the same pattern before scoping the fix — the user's named example is a sample, not an ex...
- **[conf=0.82]** Always verify from the RECEIVING end (the peer's reply, the runtime output, the cache-busted result) rather than from the SENDING end when claiming an effect occurred.
- **[conf=0.82]** Always identify what layer of indirection separates the available evidence from the ground truth; when the gap is >0 layers (log vs delivery, compile vs execute, default vs measured, cache vs fresh...

---

## designer-reviewer: reviewed files-table-reworked.png + audit-log-reworked.png — 2026-07-20 02:10

**Purpose:** Aesthetic critique of the two freshest rework screenshots in versable-builder (Speedway shell: Files page and Audit Log page), auto-selected by mtime since no path argument was given.

**Insights:**

1. When no screenshot path is passed, the project root of versable-builder is the drop zone — screenshots land as `<feature>-<state>.png` next to package.json; sorting by mtime with `fd -e png --changed-within 3d | sort by stat -f %m` disambiguates in one call.
2. The `see --ui` pre-pass mis-OCRs small dim text ("76 SKUs" read as "76 XLSXs"/"76 KIPS" in two runs) — the `--ocr` verification pass is not optional when quoting sidebar or badge strings.
3. Recurring pattern across this product's tables: a chip stacked over near-identical subtext ("Part Type" / "Part type", "CSV" / "2 KB") is what inflates rows past the 32px density cap — the fix is always inline-merge, not padding tweaks.
4. The highest-severity finding class in wide log tables is column starvation: a verbose locale timestamp (~240px) silently pushes the semantically-primary column off-canvas with no h-scrollbar affordance. Check the right edge of every table screenshot first.
5. Both screens share one token shell, so background-tint findings dedupe: file one finding per shell, reference it from the sibling review.

---

---

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-19 — 10 insights (conf>=0.80) -->

- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.82]** When a user signal (reassurance, complaint, permission) arrives, always ask 'what is the SCOPE of this signal?' separately from 'what is the DIRECTION?' — a positive signal scoped to comfort does...
- **[conf=0.82]** Always distinguish proxy evidence from direct evidence before claiming verification — if the check did not observe the actual outcome (message received, code path executed, behavior triggered, co...
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...
- **[conf=0.82]** When a user reports an issue on one instance, always grep for the full class of sibling instances sharing the same pattern before scoping the fix — the user's named example is a sample, not an ex...
- **[conf=0.82]** Always verify from the RECEIVING end (the peer's reply, the runtime output, the cache-busted result) rather than from the SENDING end when claiming an effect occurred.
- **[conf=0.82]** Always identify what layer of indirection separates the available evidence from the ground truth; when the gap is >0 layers (log vs delivery, compile vs execute, default vs measured, cache vs fresh...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-19 — 10 insights (conf>=0.80) -->

- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.85]** When the user names a specific broken instance, immediately enumerate all siblings of the same class before scoping the fix; treat the named instance as a sample that reveals a class, not as the co...
- **[conf=0.82]** Always distinguish the read axis (verify claims by reading definitions) from the write axis (express intent using the project's named abstractions) — names are untrustworthy as evidence but autho...
- **[conf=0.82]** When a user signal (reassurance, complaint, permission) arrives, always ask 'what is the SCOPE of this signal?' separately from 'what is the DIRECTION?' — a positive signal scoped to comfort does...
- **[conf=0.82]** Always distinguish proxy evidence from direct evidence before claiming verification — if the check did not observe the actual outcome (message received, code path executed, behavior triggered, co...
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...
- **[conf=0.82]** When a user reports an issue on one instance, always grep for the full class of sibling instances sharing the same pattern before scoping the fix — the user's named example is a sample, not an ex...
- **[conf=0.82]** Always verify from the RECEIVING end (the peer's reply, the runtime output, the cache-busted result) rather than from the SENDING end when claiming an effect occurred.

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-19 — 10 insights (conf>=0.80) -->

- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.82]** Always treat any context compaction or session resumption as a hard reset of ALL shared-state-mutation authorizations — re-derive push/commit/deploy permission from scratch after any compaction b...
- **[conf=0.82]** Always distinguish the read axis (verify claims by reading definitions) from the write axis (express intent using the project's named abstractions) — names are untrustworthy as evidence but autho...
- **[conf=0.82]** When a user signal (reassurance, complaint, permission) arrives, always ask 'what is the SCOPE of this signal?' separately from 'what is the DIRECTION?' — a positive signal scoped to comfort does...
- **[conf=0.82]** Always distinguish proxy evidence from direct evidence before claiming verification — if the check did not observe the actual outcome (message received, code path executed, behavior triggered, co...
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...
- **[conf=0.82]** When a user reports an issue on one instance, always grep for the full class of sibling instances sharing the same pattern before scoping the fix — the user's named example is a sample, not an ex...
- **[conf=0.82]** Always verify from the RECEIVING end (the peer's reply, the runtime output, the cache-busted result) rather than from the SENDING end when claiming an effect occurred.

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-19 — 10 insights (conf>=0.80) -->

- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.82]** Always treat context compaction as an expiration event for all shared-state-mutation authorizations — after any compaction or /catchup restoration, re-derive push/commit permission from scratch, ...
- **[conf=0.82]** Always treat any context compaction or session resumption as a hard reset of ALL shared-state-mutation authorizations — re-derive push/commit/deploy permission from scratch after any compaction b...
- **[conf=0.82]** Always distinguish the read axis (verify claims by reading definitions) from the write axis (express intent using the project's named abstractions) — names are untrustworthy as evidence but autho...
- **[conf=0.82]** When a user signal (reassurance, complaint, permission) arrives, always ask 'what is the SCOPE of this signal?' separately from 'what is the DIRECTION?' — a positive signal scoped to comfort does...
- **[conf=0.82]** Always distinguish proxy evidence from direct evidence before claiming verification — if the check did not observe the actual outcome (message received, code path executed, behavior triggered, co...
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...
- **[conf=0.82]** Avoid routing any status update, progress check, or internal concern through the user unless the message contains a decision only they can make; batch autonomous progress and halt only at genuine f...
- **[conf=0.82]** When a user reports an issue on one instance, always grep for the full class of sibling instances sharing the same pattern before scoping the fix — the user's named example is a sample, not an ex...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-19 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** When the same behavioral violation recurs more than 3 times despite advisory rules, stop adding advisory rules and escalate to a mechanical gate (hook, pre-tool check, or hard block) — advisory-o...
- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.85]** Always trigger task-list and state-persistence updates on edit count (every 5 edits or 3 tool calls) rather than on conceptual milestones — count-based triggers survive velocity spikes where judg...
- **[conf=0.82]** Always treat every context compaction or /catchup resumption as a hard reset of all shared-state-mutation authorizations — re-derive push/commit permission from scratch after any compaction bound...
- **[conf=0.82]** Always treat context compaction as an expiration event for all shared-state-mutation authorizations — after any compaction or /catchup restoration, re-derive push/commit permission from scratch, ...
- **[conf=0.82]** Always treat any context compaction or session resumption as a hard reset of ALL shared-state-mutation authorizations — re-derive push/commit/deploy permission from scratch after any compaction b...
- **[conf=0.82]** Always distinguish the read axis (verify claims by reading definitions) from the write axis (express intent using the project's named abstractions) — names are untrustworthy as evidence but autho...
- **[conf=0.82]** When a user signal (reassurance, complaint, permission) arrives, always ask 'what is the SCOPE of this signal?' separately from 'what is the DIRECTION?' — a positive signal scoped to comfort does...
- **[conf=0.82]** Always distinguish proxy evidence from direct evidence before claiming verification — if the check did not observe the actual outcome (message received, code path executed, behavior triggered, co...
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-18 — 10 insights (conf>=0.80) -->

- **[conf=0.88]** When the same behavioral violation recurs more than 3 times despite advisory rules, stop adding advisory rules and escalate to a mechanical gate (hook, pre-tool check, or hard block) — advisory-o...
- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.82]** When the same behavioral violation recurs 3+ times despite advisory rules, always escalate to a mechanical gate (a hook, a guard script, a hard tool-call block) rather than adding another advisory ...
- **[conf=0.82]** Always treat every context compaction or /catchup resumption as a hard reset of all shared-state-mutation authorizations — re-derive push/commit permission from scratch after any compaction bound...
- **[conf=0.82]** Always treat context compaction as an expiration event for all shared-state-mutation authorizations — after any compaction or /catchup restoration, re-derive push/commit permission from scratch, ...
- **[conf=0.82]** Always treat any context compaction or session resumption as a hard reset of ALL shared-state-mutation authorizations — re-derive push/commit/deploy permission from scratch after any compaction b...
- **[conf=0.82]** Always distinguish the read axis (verify claims by reading definitions) from the write axis (express intent using the project's named abstractions) — names are untrustworthy as evidence but autho...
- **[conf=0.82]** When a user signal (reassurance, complaint, permission) arrives, always ask 'what is the SCOPE of this signal?' separately from 'what is the DIRECTION?' — a positive signal scoped to comfort does...
- **[conf=0.82]** Always distinguish proxy evidence from direct evidence before claiming verification — if the check did not observe the actual outcome (message received, code path executed, behavior triggered, co...
- **[conf=0.82]** Always identify what layer a verification signal actually exercises before treating it as proof — if the signal and the outcome are decoupled (send vs receive, compile vs execute, spec vs enforce...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-18 — 10 insights (conf>=0.80) -->

- **[conf=0.92]** Always treat every context compaction or session resumption as a hard reset of all shared-state-mutation authorizations — re-derive push/commit/deploy permission from scratch after any compaction...
- **[conf=0.88]** Avoid adding another advisory rule for a pattern that has recurred more than 5 times under advisory-only enforcement — escalate to a mechanical gate (hook, CLI guard, pre-push check) as the only ...
- **[conf=0.88]** When the same behavioral violation recurs more than 3 times despite advisory rules, stop adding advisory rules and escalate to a mechanical gate (hook, pre-tool check, or hard block) — advisory-o...
- **[conf=0.85]** Before interrupting the user, apply a decision-point test: can this be resolved autonomously or deferred? If yes, do so; if no, present a self-contained question with enough context to answer in on...
- **[conf=0.82]** When the same behavioral violation recurs 3+ times despite advisory rules, always escalate to a mechanical gate (a hook, a guard script, a hard tool-call block) rather than adding another advisory ...
- **[conf=0.82]** Always treat every context compaction or /catchup resumption as a hard reset of all shared-state-mutation authorizations — re-derive push/commit permission from scratch after any compaction bound...
- **[conf=0.82]** Always treat context compaction as an expiration event for all shared-state-mutation authorizations — after any compaction or /catchup restoration, re-derive push/commit permission from scratch, ...
- **[conf=0.82]** Always treat any context compaction or session resumption as a hard reset of ALL shared-state-mutation authorizations — re-derive push/commit/deploy permission from scratch after any compaction b...
- **[conf=0.82]** Always distinguish the read axis (verify claims by reading definitions) from the write axis (express intent using the project's named abstractions) — names are untrustworthy as evidence but autho...
- **[conf=0.82]** When a user signal (reassurance, complaint, permission) arrives, always ask 'what is the SCOPE of this signal?' separately from 'what is the DIRECTION?' — a positive signal scoped to comfort does...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-18 — 10 insights (conf>=0.80) -->

- **[conf=0.92]** Always re-derive all push/deploy/shared-state-mutation prohibitions from durable sources (CLAUDE.md, project settings) immediately after any context compaction or session resumption, treating compa...
- **[conf=0.92]** Always treat every context compaction or session resumption as a hard reset of all shared-state-mutation authorizations — re-derive push/commit/deploy permission from scratch after any compaction...
- **[conf=0.88]** Always treat every context compaction or session resumption as a hard reset of all push/deploy authorizations — re-derive prohibitions from durable sources (CLAUDE.md, protected-repos.list) befor...
- **[conf=0.88]** Avoid adding another advisory rule for a pattern that has recurred more than 5 times under advisory-only enforcement — escalate to a mechanical gate (hook, CLI guard, pre-push check) as the only ...
- **[conf=0.88]** When the same behavioral violation recurs more than 3 times despite advisory rules, stop adding advisory rules and escalate to a mechanical gate (hook, pre-tool check, or hard block) — advisory-o...
- **[conf=0.85]** Always treat terse continuation signals as authorizing only local, reversible side-effects (file edits, local builds, scratch files); never interpret them as authorization for shared-state mutation...
- **[conf=0.82]** When the same behavioral violation has been recorded 3+ times despite existing advisory rules, avoid adding another advisory rule — instead implement or request a mechanical gate (a hook, a CLI g...
- **[conf=0.82]** Avoid adding another advisory memory/rule for a pattern that already has 5+ advisory entries — instead, invest the effort in a mechanical gate (a hook, a CLI guard, a data-path check) that enforc...
- **[conf=0.82]** When the same behavioral violation recurs 3+ times despite advisory rules, always escalate to a mechanical gate (a hook, a guard script, a hard tool-call block) rather than adding another advisory ...
- **[conf=0.82]** Always treat every context compaction or /catchup resumption as a hard reset of all shared-state-mutation authorizations — re-derive push/commit permission from scratch after any compaction bound...

---


## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-07-17 — 10 insights (conf>=0.80) -->

- **[conf=0.92]** Always re-derive all push/deploy/shared-state-mutation prohibitions from durable sources (CLAUDE.md, project settings) immediately after any context compaction or session resumption, treating compa...
- **[conf=0.92]** Always treat every context compaction or session resumption as a hard reset of all shared-state-mutation authorizations — re-derive push/commit/deploy permission from scratch after any compaction b...
- **[conf=0.88]** Always treat every context compaction or session resumption as a hard reset of all push/deploy authorizations — re-derive prohibitions from durable sources (CLAUDE.md, protected-repos.list) before ...
- **[conf=0.88]** Avoid adding another advisory rule for a pattern that has recurred more than 5 times under advisory-only enforcement — escalate to a mechanical gate (hook, CLI guard, pre-push check) as the only in...
- **[conf=0.88]** When the same behavioral violation recurs more than 3 times despite advisory rules, stop adding advisory rules and escalate to a mechanical gate (hook, pre-tool check, or hard block) — advisory-onl...
- **[conf=0.85]** Always treat terse continuation signals as authorizing only local, reversible side-effects (file edits, local builds, scratch files); never interpret them as authorization for shared-state mutation...
- **[conf=0.82]** When the same behavioral violation has been recorded 3+ times despite existing advisory rules, avoid adding another advisory rule — instead implement or request a mechanical gate (a hook, a CLI gua...
- **[conf=0.82]** Avoid adding another advisory memory/rule for a pattern that already has 5+ advisory entries — instead, invest the effort in a mechanical gate (a hook, a CLI guard, a data-path check) that enforces...
- **[conf=0.82]** When the same behavioral violation recurs 3+ times despite advisory rules, always escalate to a mechanical gate (a hook, a guard script, a hard tool-call block) rather than adding another advisory ...
- **[conf=0.82]** Always treat every context compaction or /catchup resumption as a hard reset of all shared-state-mutation authorizations — re-derive push/commit permission from scratch after any compaction boundar...

---

