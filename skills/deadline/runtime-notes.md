
## run: speedway product-team freeze [vb-fbl-96] — 2026-07-18

T 03:00 IST · landed 02:45 (target 02:47, 15% margin held). Final DRIFT
~0.9x. GROWTH +7 parts (2 arbitration tasks ringed OUT of window at
intake; a 5-item owner batch at T-55m absorbed in-window in ~25m).
User turns consumed in-window: 2 (the arm/batch exchange rode the owner's
own return; the npm-publish halt). Both load-bearing — attention-yield
held.

Assumptions vetoed by the user: none (4 veto-ledger entries, none
flipped). Worked well: rings pre-shedding POLISH at arm time; the
one-command halt brief (owner returned, ran publish, said two words);
heartbeat cron as wake defense (3 firings, all useful). Learned:
(1) registry-published kit vs workspace-linked dev masks missing-symbol
breakage until a deploy — smoke the DEPLOYED artifact, not just dev,
before calling any icon/kit-touching change done; (2) npm publish is
classifier-blocked — route kit releases through the owner as a planned
step, not a discovered halt; asking a peer post-denial is the same
intent and also blocks; (3) zsh: never `for path in …` — it clobbers
PATH (lowercase path is tied).
